# План: языковой сервис wdl — редакторы, автодополнение, отладка, профилирование

Подсветка, автодополнение, анализ, переход к объявлению, запуск, точки останова
и профиль — в IntelliJ IDEA, в VS Code и внутри приложения, которое встроило язык
и дало пользователю редактор. Документ отвечает на три вопроса: что в коде уже готово
к этому, чего не хватает и в каком порядке это делать.

Прошлая редакция называлась «План: плагин IntelliJ IDEA для wdl» и целила в одну IDE.
Причина смены рамки — в разделе «Что изменилось против прошлой редакции»: плагин
остаётся в плане, но перестаёт быть первым шагом.

## Вердикт

**Делать можно, и большая часть работы — не в редакторе, а здесь, в ядре.**

Ядро оказалось подготовлено к инструментам лучше, чем это обычно бывает: `Span`
в каждом токене и каждом узле, разбор, который не падает на первой ошибке и всегда
возвращает дерево, узлы-`record`'ы без единой строки логики, посетители с проверяемой
компилятором полнотой, ноль изменяемой статики и ноль внешних зависимостей. Всё это
ровно те свойства, из-за отсутствия которых обычно переписывают лексер и парсер второй
раз — специально для IDE. Здесь этого делать не придётся.

Не хватает при этом не мелочей, а одного целого слоя. **Статической модели имён
в языке нет по решению**: `Resolver` был и убран, имя ищется в момент выполнения,
а `runtime.Declarations` — диагностика на неудачном пути, а не индекс. Автодополнение
переменных и функций — это ровно та стадия, которой сознательно нет. Значит, её надо
завести заново, но **как инструмент, а не как стадию конвейера**: анализатор
в `wdl-tools`, который читает то же дерево и ничего не знает про выполнение. Инвариант
«между разбором и выполнением стадии нет» при этом не нарушается — на пути запуска
скрипта нового кода не появляется.

Отладка и профилирование готовы лучше, чем можно было ожидать, потому что у выполнения
уже есть подходящие воронки: большая часть инструкций проходит через
`StmtVisitor.visit`, а вызовы — через `ExecutionContext.call`, где создаётся цепочка
`Frame`. Есть одно важное исключение: верхнеуровневый `ExprStmt` вычисляется напрямую
в `Interpreter.statements` и общий `visit` обходит. Поэтому корректный хук обязан
покрыть и диспетчер visitor, и этот специальный путь. Второй интерпретатор, байткод
и изменяемая статика всё равно не нужны.

Оценка объёма: ~70% работы — в этом репозитории (лексер, дополнения AST, слой анализа,
хук отладчика, сэмплер), ~30% — адаптеры (LSP, DAP, плагин). Первый переносимый
редакторный результат появляется после этапа 6, а полноценный основной сценарий
в IntelliJ — после этапа 8.

## Исходная точка на 4 сентября 2026 года

Проверка `./gradlew test --rerun-tasks` проходит. В сохранённых JUnit-результатах:
75 suites, 1268 tests, 0 failures, 0 errors, 0 skipped. Есть три compiler warning,
не связанные с семантикой языка: один про экспорт возвращаемого `MemberTable`
и два про неявные публичные конструкторы в экспортированном пакете.

Проценты ниже — инженерная оценка оставшейся работы, а не покрытие тестами:

| Подсистема | Текущая готовность | Что уже есть | Главный остаток |
|---|---:|---|---|
| Лексер/парсер как база IDE | 75% | error recovery, AST, spans | lossless tokens и инварианты |
| Синтаксическая диагностика | 65% | список severity/message/span | codes, versioned publication |
| Подсветка | 40% | стабильные виды токенов | trivia, bad token, editor adapter |
| Symbols/completion/navigation | 10–15% | AST и отдельные name spans | вся статическая модель имён |
| Workspace/import analysis | 25–30% | `ModuleSource`, `ModuleKey` | overlay, graph, index |
| Встраивание runtime | 70–80% | `WdlEngine`, modules, globals | tooling catalog и session API |
| IntelliJ-интеграция | 5–10% | внешний file type/tool config | настоящий plugin и LSP client |
| Отладка | около 10% | frames, call depth, threads | hooks, snapshots, DebugSession, DAP |
| Профилирование | около 10% | pipeline metrics | logical stacks, sampler, export |

Итоговая готовность к полноценной универсальной IDE-интеграции — примерно 20–30%.
Это хороший результат для языкового ядра: переписывать компилятор не требуется,
но analysis/debug tooling предстоит построить как новые подсистемы.

Ориентир для одного разработчика при последовательной работе:

- editor MVP с LSP и базовой IDEA-интеграцией — 4–8 недель;
- workspace semantics, formatter, rename и catalogs — ещё 1–2 месяца;
- многопоточный debugger/DAP — ещё 1–2 месяца;
- profiler и полировка embedded editor — отдельный следующий релиз.

## Что изменилось против прошлой редакции

Прошлая редакция выбирала между тремя вариантами (переписать грамматику под IntelliJ /
LSP-сервер / два слоя с одной грамматикой) и рекомендовала третий: нативное PSI, LSP
отвергнут «из-за потолка возможностей» — рефакторинги, структура и свёртка через LSP
либо бедные, либо отсутствуют.

Вывод остаётся верным **как конечная точка** и оказался неверным **как первый шаг**,
потому что к требованиям добавилось встраивание в чужие места: VS Code и — главное —
редактор внутри приложения, которое встроило язык. Нативное PSI не встраивается никуда,
кроме IDEA, а смысловой слой, из которого оно берёт ответы, встраивается везде.

Отсюда три правки:

1. **Смысловой слой — не деталь плагина, а самостоятельный продукт.** Обычный Java API
   в терминах `Span`, без единой строки про LSP и IntelliJ. Его потребители равноправны:
   LSP-сервер, плагин IDEA, приложение со своим редактором, REPL.
2. **LSP идёт раньше нативного плагина.** Он даёт несколько редакторов за один адаптер,
   включая саму IDEA (см. развилку ниже), и служит проверкой того, что смысловой слой
   действительно не зависит от хозяина.
3. **Отладка — DAP, а не свой протокол.** Прошлая редакция предлагала «сокет и свой
   маленький протокол». Свой протокол экономит день работы и забирает две интеграции:
   VS Code и Neovim умеют DAP и не умеют наш сокет.

Добавились два раздела, которых в прошлой редакции не было вовсе: отладка разобрана
до места вставки хука и политики останова, а профилирование появилось как отдельная
работа — время стадий отвечает на «сколько шёл скрипт», а не на «где он стоял».

## Что уже готово и почему это важно

| Что есть | Почему это экономит работу |
|---|---|
| `Span(start, end)` в каждом токене и узле | Смещения IntelliJ — такие же индексы в `CharSequence`, позиции LSP считаются в тех же единицах UTF-16. `Span` ↔ `TextRange` ↔ `Position` переводится без пересчёта. |
| Разбор не прерывается на ошибке, на месте узла остаётся `ErrorExpr`/`ErrorStmt` | Редактор почти всегда показывает недописанный файл. Парсер, падающий на первой ошибке, для IDE непригоден в принципе. |
| `Diagnostics` — список `(severity, message, span)`, `Source.positionOf` рядом | Готовый источник и для `Annotator`, и для `publishDiagnostics`: перевод один в один. |
| Лексер **без состояния между токенами** — интерполяции строк в языке нет | Восстановление с середины файла (`start(buffer, from, to, state)` у IntelliJ) не требует работы: состояние всегда 0, разбор куска даёт то же, что разбор целого. |
| Узлы — неизменяемые `record`'ы, дерево ничего не знает о выполнении | Дерево кэшируется на файл и читается из любого потока — а IntelliJ читает индекс из фоновых потоков всегда. |
| Ноль изменяемой статики, ноль зависимостей у `wdl-core` | Потребитель кладёт `wdl-core.jar` и `wdl-tools.jar` к себе и не тащит транзитивный ад. Два открытых проекта не мешают друг другу. |
| `nameSpan` уже есть у `const`, `class`, `trait`, `import`, `for ... in`, `catch`, `use`, `property`, именованного аргумента | Половина точек «перейти к объявлению» и «переименовать» уже адресуема. |
| `AccessStyle`, `BodyStyle`, `ReturnStyle`, `TryStyle`, `Token.afterNewline()`, `Token.quoted()` | Форма записи сохранена — форматтер и свёртка имеют, из чего восстановить исходный вид. |
| `x.` с недописанным именем даёт `AccessExpr(target, ErrorExpr, DOT, ...)` | Самый частый случай автодополнения — получатель разобран и доступен, гадать по токенам не надо. |
| `BuiltinMembers.of(ValueType)` + `MemberSet.names()` | Список членов каждого типа (`a.size`, `text.upper()`) перечислим статически, **без запуска**. |
| Цепочка `Frame`: имя функции, место вызова, файл вызывающего, `depth()`, `parent` | Готовый стек для отладчика **и** готовое дерево вызовов для сэмплирующего профилировщика. Звенья неизменяемы — читаются из другого потока. |
| `ExecutionContext.callDepth()` | `step over` и `step out` считаются сравнением глубины; отдельный учёт не нужен. |
| Почти единая воронка инструкций: `StmtVisitor.visit(Stmt, C)` — `default`-switch | Хук отладчика ставится в одно переопределение плюс в специальный путь верхнеуровневого `ExprStmt`; двадцать четыре метода visitor править не нужно. |
| `Output` и `Metrics` в `ExecutionContext` с заглушками по умолчанию | Готовый образец для `Debugger` и `Profiler`: та же форма, та же цена по умолчанию — ноль. |
| `Metrics`/`Stage`, флаги `--tokens`, `--ast`, `--metrics`, `--metrics-each` | Время стадий уже мерится и выводится; профилировщику остаётся своя гранулярность, а не вся инфраструктура. |
| `ModuleKey.resolve(path, home)` — чистая текстовая функция | Переход по `import lib.math` к файлу считается без файловой системы, на VFS редактора. |
| Диагностика печатается как `файл:строка:столбец: ошибка: ...` | Стандартный `Filter` консоли IDEA превращает это в кликабельную ссылку почти без кода. |

## Чего не хватает

### 1. Лексер теряет текст и не сообщает об ошибках токеном

`Lexer.lineComment()` и `blockComment()` просто двигают `pos`; пробелы пропускаются;
неизвестный символ уходит в `Diagnostics`, а в потоке токенов не остаётся ничего.
Поток токенов не покрывает файл.

Ограничение блокирующее сразу для четверых: лексер подсветки IntelliJ обязан отдавать
токены встык от 0 до конца документа (дыра — это либо исключение, либо «пропала
половина файла»); `semanticTokens` в LSP считает по тем же правилам; форматтер молча
съел бы все комментарии; подсказке по наведению документацию брать неоткуда.
Одна правка закрывает четыре задачи.

Нужно:

* виды токенов `LINE_COMMENT`, `BLOCK_COMMENT`, `WHITESPACE`, `BAD_CHARACTER`;
* режим лексера, в котором они попадают в поток, и режим по умолчанию, где всё как
  сейчас, — парсер не должен фильтровать поток на каждом запуске скрипта, у него
  другая цена;
* `BAD_CHARACTER` вместо молчания на неизвестном символе — подсветка не имеет доступа
  к `Diagnostics` и должна узнавать о мусоре из самого токена.

Потоковый `next()` — оптимизация на потом, не условие старта.

### 2. У части объявлений нет `nameSpan`

Без интервала самого имени нельзя ни перейти к объявлению, ни переименовать,
ни подсветить употребления. Не хватает у:

* `FunctionExpr` — у имени функции интервала нет вообще, а это главный случай;
* `ClassDeclStmt.Factory`, `TraitDeclStmt.Requirement`, `ExtendStmt.label`.

У `FunctionExpr.Param` текущий `span` уже строится из `name.span()` и покрывает
именно имя параметра. Его менять не требуется; нужен отдельный тест, закрепляющий
этот контракт, чтобы будущая правка парсера не расширила интервал до значения
по умолчанию.

### 3. По дереву нельзя пройти единообразно

`Node` даёт только `span()`. Списка детей нет, поэтому каждый инструмент пишет обход
заново: `AstDumper` — свой, `runtime.Declarations` — свой и намеренно неполный (там
перечислены не все виды инструкций, потому что для подсказки хватало). Для редактора
обход — самая переиспользуемая операция: «узел под курсором», «путь до корня»,
«все употребления имени», «структура файла», «свёртка».

Мешает и то, что части дерева узлами не являются: `Param`, `Rest`, `Argument`,
`Decorator`, `ObjectExpr.Entry`, `ClassDeclStmt.Superclass`/`TraitRef`/`Factory`,
`PropertyDecl` и её `Accessor`, `TryStmt.Catch`, `UseStmt.Binding`,
`TraitDeclStmt.Requirement`. У всех есть `span`, но по типу они не родня, и обход
обязан знать каждого в лицо.

Решение: ввести в закрытую иерархию третьего наследника — `sealed interface Fragment
extends Node`, разрешающий эти записи. Тогда «у любого куска дерева есть `span`»
становится правдой без исключений, а `switch` по `Expr`/`Stmt` от этого не меняется:
это разные ветви `Node`, и полнота их разборов не страдает. Сверху — `ast.Nodes`
с `children(Node)`, `walk(Node, visitor)`, `at(Program, offset)` и `pathTo(Program, offset)`.

### 4. Интервал ребёнка умеет вылезать за интервал родителя

`Parser.access()`, строка 1982: при `obj.` с неподходящим следующим токеном
возвращается

```java
return new AccessExpr(target, new ErrorExpr(name.span()), AccessStyle.DOT,
        target.span().to(operator.span()));
```

— интервал родителя кончается на точке, а интервал ребёнка начинается после неё.

Пока дерево только печатают, это незаметно. Для редактора это ломает `at(offset)`
и, если идти по пути «проигрывания» AST в PSI, — само построение PSI по интервалам.

Правка: пустой узел-ошибка **в точке после точки**, а не на чужом токене —
`new ErrorExpr(Span.point(operator.span().end()))`. Именно так, а не расширением
родителя до `name.span()`: подвернувшийся токен нередко принадлежит следующей
инструкции (`obj.` в конце строки, `foo()` на следующей), и проглатывать его в узел
обращения значит соврать про структуру файла там, где редактор как раз и работает.

Нужен инвариант уровня теста: **интервал ребёнка вложен в интервал родителя, дети
идут слева направо и не пересекаются**, — с прогоном по всем 47 файлам `examples/`,
включая `lexer-errors.wdl`, и по набору нарочно недописанных фрагментов.

### 5. Нет статической модели имён

Главный пробел. Чтобы предложить список переменных, надо знать, какие имена видны
в этой точке файла, а такого знания в проекте нет ни в каком виде.

Особенность языка, которую модель обязана воспроизвести дословно: **имя существует
с той строки, где его завели**, но тело функции видит и то, что объявлено ниже, —
потому что имя ищется в момент вызова. То есть видимость в точке — это (объявления
выше в этой области) + (весь верхний уровень файла, если мы внутри тела функции)
+ импортированное + корневая область. Ошибиться здесь значит либо не предлагать
рекурсию, либо предлагать переменную, которой на этой строке ещё нет.

### 6. Нельзя перечислить ни область, ни встроенные имена

`Environment` умеет `lookup`, `lookupHere`, `isDefined`, `define` — и всё; перечисления
содержимого нет. `Builtins.installTo(scope)` кладёт имена в область, но списка имён
не отдаёт; библиотеки `wdl-stdlib` устроены так же: `Library` определяет имена
в области, и узнать их состав, не выполнив установку, нельзя.

Из этого следует сразу три недостачи: автодополнение `println`, `typeof`, `std.`,
`io.` брать неоткуда; в REPL нет дополнения по своим же именам; **у отладчика нечего
показать в панели переменных** — кадр есть, а перечислить его область невозможно.

Нужно:

* `Environment.names()` (и `namesHere()`) — закрывает REPL и отладчик;
* **статический каталог** для библиотек: имя, вид, сигнатура, короткое описание, —
  объявленный рядом с самой библиотекой (кто определяет имена, тот их и описывает),
  и интерфейс каталога в `wdl-tools`, чтобы анализатор не зависел от `wdl-stdlib`.
  Направление зависимостей не меняется: каталог `sys.*` подаёт снаружи тот, кто про
  них знает. Заодно это первый машинный ответ на вопрос «что вообще есть в `sys.io`»,
  которого сейчас нет нигде, кроме документации.

### 7. У диагностик нет кодов

`Diagnostics.error(span, "текст")` — сообщения свободным текстом. Для редактора это
значит: нельзя подавить конкретную проверку, нельзя привязать быструю правку к виду
ошибки, нельзя завести настройку строгости, нельзя сослаться на объяснение.

Старт не блокирует, но цена растёт со временем: сейчас это 157 мест вызова
`diagnostics.error/warning` в ядре и 120 `new WdlRuntimeError`. Поле-перечисление
вводить надо сейчас, пока их столько.

### 8. Нет ни точки для отладчика, ни способа снять профиль

Точки останова требуют, чтобы кто-то мог вклиниться перед выполнением инструкции;
профиль — чтобы кто-то мог узнать, где скрипт находится. Ни того, ни другого нет.
Куда это вставляется и чего стоит — разделы «Отладка» и «Профилирование».

## Архитектура: один смысл, пять адаптеров

```
                     ┌────────────────────────────────────────┐
                     │ wdl-tools/analysis + tooling sessions  │
                     │ symbols · types · debug · profile API  │
                     └────────────────────────────────────────┘
                         ▲        ▲        ▲        ▲       ▲
                         │        │        │        │       │
                      wdl-lsp  wdl-dap   IDEA   embedded   CLI
                        LSP      DAP     plugin    editor   JSON/REPL
```

Смысловой слой отвечает на вопросы в терминах `Span` и ничего не знает о том, кто
спрашивает. Это и есть та основа, которая встраивается в разные места: приложение
со встроенным языком берёт её напрямую, в своём же процессе, без сокета
и сериализации; LSP-сервер — тонкий перевод её ответов в JSON-RPC; плагин IDEA —
тонкий перевод в свои точки расширения.

Развилка синтаксиса из прошлой редакции сохраняется в том же виде, но переезжает
на последний этап: **одна грамматика, два слоя**. Лексер IntelliJ — обёртка над нашим
в режиме с тривией; `PsiParser` **не разбирает заново**, а «проигрывает» готовое
дерево: зовёт `Parser.parseProgram`, а затем идёт по токенам и открывает/закрывает
маркеры `PsiBuilder` по интервалам узлов. Получается настоящее PSI, построенное из
нашего AST, — ради этого и нужен инвариант вложенности интервалов (пункт 4).

Смещения совпадают буквально: `Span` — индексы в `String` (единицы UTF-16), документ
IntelliJ — `CharSequence` с теми же единицами и нормализованными `\n`, позиции LSP
по умолчанию тоже считаются в UTF-16. Оговорки две: BOM (лексер пропускает его
в позиции 0, документ IDEA его уже не содержит — лексить надо текст документа, а не
байты файла) и нумерация с нуля в LSP против единицы в `Position`.

### Развилка, которую надо проверить до этапа 3

**Какую сборку IntelliJ нужно поддерживать.** Публичная LSP-интеграция доступна
в обычных коммерческих продуктах JetBrains, включая IntelliJ IDEA; начиная с линии
2025.x она не требует активной платной подписки в установленной IDEA. Но её нет
в open-source-сборках IntelliJ Platform и Android Studio. Для этих целей нужен
нативный клиент либо отдельно проверенная сторонняя интеграция.

Для основной актуальной IntelliJ IDEA LSP-путь даёт почти весь первый сценарий
разработки, поэтому нативный PSI можно отложить. Перед началом плагина всё равно
надо зафиксировать конкретные `sinceBuild`/target build: LSP-API платформы активно
развивается, а в 2026.1.4 часть классов и extension points была переименована.

## Изменения в этом репозитории

### `wdl-core`

| Задача | Файлы | Заметки |
|---|---|---|
| Виды токенов для тривии и мусора | `lexer/TokenType.java` | `LINE_COMMENT`, `BLOCK_COMMENT`, `WHITESPACE`, `BAD_CHARACTER` — с новым `Kind.TRIVIA`, чтобы `isKeyword`/`isOperator` не поехали |
| Режим лексера с тривией | `lexer/Lexer.java` | второй `tokenize(...)` с режимом; путь по умолчанию не меняется ни на строку |
| `nameSpan` там, где его нет | `ast/expr/FunctionExpr.java`, `ast/stmt/ClassDeclStmt.Factory`, `TraitDeclStmt.Requirement`, `ExtendStmt` | `Param.span` уже равен интервалу имени; для него нужен только контрактный тест |
| `Fragment` в иерархию `Node` | `ast/Node.java` + перечисленные вложенные записи | разборы по `Expr`/`Stmt` не затрагиваются |
| Единый обход | новый `ast/Nodes.java` | `children`, `walk`, `at(offset)`, `pathTo(offset)` |
| Инвариант интервалов | `parser/Parser.java:1982` и тест | ребёнок вложен в родителя, дети не пересекаются |
| Перечисление области | `runtime/Environment.java`, `Scope`, `ModuleScope`, `InstanceScope`, `FieldScope` | `names()`, `namesHere()` — закрывает и REPL, и панель переменных отладчика |
| Код диагностики | `diagnostic/Diagnostic.java` | новое поле-перечисление; вводить сразу, править потом дороже |
| Хук отладчика | `runtime/Debugger.java` (новый), `ExecutionContext`, `Interpreter.visit(Stmt, …)`, `Interpreter.statements` | заглушка `Debugger.off()` по умолчанию; верхнеуровневый `ExprStmt` покрывается отдельно |
| Область в кадре | `runtime/Frame.java`, `ExecutionContext.call` | одно поле: без него внешние кадры нечем наполнить |
| Публикация текущего кадра | `runtime/Run.java`, `RunThreads` | реестр «поток → слот с кадром» для сэмплера |
| `exports ru.wds.wdl.runtime.members` | `module-info.java` | `BuiltinMembers` становится частью API инструментов |

### `wdl-tools` — слой анализа (пакет `ru.wds.wdl.tools.analysis`)

Модуль для этого заводить не надо: `wdl-tools` ровно для этого и объявлен
(«линтер, форматтер, дамп AST, позже LSP») и зависит только от ядра.

* `FileAnalysis` — результат разбора одного файла: дерево, диагностика, дерево областей,
  таблица символов, комментарии по интервалам. Неизменяем, кэшируется потребителем
  по «файл + версия документа».
* `ScopeTree` — области файла (файл, тело функции, блок, цикл, класс, трейт, `use`,
  `catch`) с объявленными в каждой символами.
* `Symbol` — имя, вид, `nameSpan`, интервал объявления, документация. Виды: переменная,
  константа, функция, параметр, класс, трейт, метод, свойство, фабрика, псевдоним
  модуля, переменная цикла, переменная `catch`.
* `visibleAt(offset)` — правило видимости из пункта 5, включая «тело функции видит
  верхний уровень целиком».
* `resolve(offset)` — символ под курсором, объявление или употребление.
* `usages(symbol)` — употребления в файле.
* `Comments` — комментарии, привязанные к следующему объявлению: источник подсказки
  по наведению.
* `Catalog` — интерфейс внешних имён (встроенные функции, `std`, `sys.*`); реализация
  подаётся снаружи.
* `Types` (по силам) — тип получателя там, где он виден из текста: литерал,
  `new C(...)`, массив, объект, переменная с единственным присваиванием, модуль.
  Промах — не ошибка: тогда после точки предлагаются члены всех типов, как это делают
  IDE для JS.
* `DebugSession` — сессия отладки поверх ядра, независимая от протокола: точки
  останова, шаги, кадры, вычисление выражения. См. «Отладка».

### `wdl-lsp` — новый модуль

Зависит от `core`, `tools` и `stdlib` (за каталогами `std` и `sys.*`) и держит
у себя зависимость LSP4J. Ядро остаётся чистым, `checkNoRuntimeDependencies` не
трогается; версия LSP4J объявляется в `gradle/libs.versions.toml`.

Внутри находится только перевод `FileAnalysis` и запросов workspace в LSP DTO,
управление жизненным циклом документов и транспорт stdio/socket. Семантических
правил языка здесь нет.

### `wdl-dap` — новый модуль

Зависит от `core` и протокол-независимой `DebugSession` из `tools`. Содержит
DAP lifecycle, таблицу `variablesReference`, запуск/attach процесса и перевод
событий отладчика в DAP. Разделение с `wdl-lsp` обязательно: языковой сервер живёт
на весь workspace, debug adapter — на отдельную сессию запуска; у них разный срок
жизни, потоки и каналы вывода.

### `wdl-stdlib`

* `Sys.catalog()` и `Std.catalog()` — статическое описание модулей и их имён рядом
  с самими модулями.

### `wdl-cli`

* `--json` у `--tokens`/`--ast`/`--metrics`: машинный вывод для конфигураций запуска
  и для внешних инструментов. Писать через `JsonWriter` из `wdl-stdlib`, отдельная
  зависимость не нужна.
* `--debug-port`/`--wait-for-debugger` — запуск под отладчиком.
* `--profile=samples|calls|lines`, `--profile-out <файл>` — снятие профиля.
* дополнение в REPL по `Environment.names()` — первый потребитель модели имён,
  и самый дешёвый.

## Автодополнение: что именно считать

Три источника, и они складываются, а не выбираются:

1. **Имена в точке** — `visibleAt(offset)` из слоя анализа. Правило видимости в языке
   нестандартное (пункт 5), и модель обязана повторить его дословно, иначе автор либо
   не увидит рекурсию, либо получит переменную, которой на этой строке ещё нет.
2. **Члены после точки** — три случая. Тип получателя виден из текста → члены этого
   типа из `BuiltinMembers`/`ClassShape`/каталога модуля. Не виден → члены всех типов.
   `x.` с недописанным именем уже разобран в `AccessExpr(target, ErrorExpr, DOT)`,
   получатель доступен, гадать по токенам не надо.
3. **Ключевые слова и пути модулей** — по позиции в дереве: после `import` — пути через
   `ModuleKey.resolve`, в начале инструкции — ключевые слова, после точки — никогда.

Планка по типам сознательно низкая: язык динамический, честного вывода типов не будет.
Обещать больше значит однажды предложить `text.push`.

Отдельно стоит помнить, что **члены бывают снимками**: `Member.snapshot()` уже
различает «свойство» и «снимок», и подсказка обязана это показывать — иначе автор
положит снимок в переменную и будет удивлён. Это же различие даёт инспекцию
«снимок в условии цикла».

## Отладка

### Куда встаёт хук

Основная диспетчеризация инструкций идёт через `StmtVisitor.visit(Stmt, C)`, поэтому
`Interpreter` может переопределить его и поставить общий хук:

```java
@Override
public Void visit(Stmt stmt, ExecutionContext context) {
    beforeStatement(stmt, context);
    return StmtVisitor.super.visit(stmt, context);
}

private void beforeStatement(Stmt stmt, ExecutionContext context) {
    ExecutionInstrumentation instrumentation = context.instrumentation();
    if (instrumentation.enabled()) {
        instrumentation.beforeStatement(stmt, context.debugView());
    }
}
```

Но в `Interpreter.statements` верхнеуровневый `ExprStmt` вычисляется напрямую
ради сохранения значения последнего выражения. Перед этим вычислением надо вызвать
тот же `beforeStatement`. Тест должен перечислить все виды `Stmt` и доказать,
что каждый реально исполненный узел публикует событие ровно один раз.

Инструментация ложится в `Run`/`ExecutionContext` рядом с `Output` и `Metrics`,
имеет заглушку `off()` и не использует изменяемую статику. Горячий путь получает
одну предсказуемую проверку плюс специальный вызов для верхнего уровня. Стоимость
нужно измерить benchmark-сценарием до и после. Шаги по выражениям следует включать
отдельным тяжёлым режимом: вызовов `ExprVisitor.visit` существенно больше.

### Чего не хватает вокруг хука

* **Кадр не знает своей области.** `Frame` держит `function`, `callSite`, `unit`,
  `parent` — панель «переменные» для внешних кадров из этого не собрать. Правка
  на одно поле: `ExecutionContext.call` уже получает `scope` вызываемого аргументом,
  достаточно передать его в `Frame.of`. Плюс `Environment.names()` из пункта 6 —
  иначе перечислять в кадре нечего.
* **`Frame` пакетно-приватен, а `trace()` отдаёт строки.** Отладчику нужен снимок:
  `List<StackFrame>` с именем, файлом, интервалом и областью. Строки для трассировки
  ошибки остаются как есть — это другой потребитель.

### Шаги

Считаются глубиной, отдельного учёта не нужно: `callDepth()` уже есть.

| Шаг | Условие остановки на следующем `beforeStatement` |
|---|---|
| `step in` | любая глубина |
| `step over` | `depth <= depth₀` |
| `step out` | `depth < depth₀` |
| `continue` | попадание в точку останова |

Точка останова адресуется «файл + строка». Перевод строки в интервал — `Source`,
и он же даёт обратный перевод для показа: `positionOf(span.start())`.

Условная точка останова и «вычислить выражение» — это `Lexer` + `Parser.expression`
+ `Interpreter` поверх `frame.scope()`. Всё уже есть; нового кода — «разобрать строку
как выражение и выполнить в этом контексте», с оговоркой, что вычисление обязано идти
через `Foreign`-правило: чужая ошибка внутри вычисления не должна убивать сессию.

### Потоки — самое неприятное

Внутри запуска работает сколько угодно потоков, глобального замка нет (`docs/threads.md`).
Значит, у отладчика обязана быть **политика останова**, и выбрать её надо в проекте,
а не в реализации:

* `THREAD` — встал только тот поток, который попал в точку останова; остальные бегут.
  Дёшево и почти всегда не то, что человек имел в виду.
* `ALL` — встали все. На языке без GIL это не флаг, а точка сбора: потоки съезжаются
  в `beforeStatement`, а тот, кто в это время сидит в чужом Java-вызове (сокет, GUI,
  сон), приедет позже или не приедет вовсе. Учёт живых потоков есть — `RunThreads`
  их знает.

Решение: реализовать `THREAD` первым, `ALL` — вторым и с честным таймаутом сбора
(«остановлено 3 из 4 потоков»), а не делать вид, что мир замер.

Остановка на ошибке (`ErrorKind`, брошенный `WdlError`) — третий вид точки останова,
и вставляется он не в `visit`, а туда, где ошибка создаётся: у `WdlRuntimeError`
и в `Foreign.call`. Это то, чего от отладчика скриптового языка ждут в первую очередь,
и стоит она почти ничего — путь и без того холодный.

### Протокол

**DAP, а не свой протокол.** Для Java можно использовать модели
`org.eclipse.lsp4j.debug`, но транспорт и lifecycle должны жить в отдельном
`wdl-dap`. VS Code, Neovim и другие клиенты получают стандартный DAP. IntelliJ
подключается нативным `XDebugger`-мостом поверх той же `DebugSession` либо через
проверенный публичный DAP-host API целевой версии платформы.

Соответствие получается почти механическим, и это признак того, что модель выбрана
верно:

| DAP | Наше |
|---|---|
| `setBreakpoints` | строка → `Span` через `Source` |
| `stackTrace` | цепочка `Frame` |
| `scopes` / `variables` | `Frame.scope()` + `Environment.names()` |
| `evaluate` | `Parser.expression` + `Interpreter` в контексте кадра |
| `next` / `stepIn` / `stepOut` | сравнение `depth` |
| `threads` | `RunThreads` |
| `output` | `Output` — он и так уже точка расширения |

Порядок работ внутри этапа: instrumentation в ядре → `DebugSession` в `wdl-tools`
(протокол-независимая) → DAP-обёртка в `wdl-dap` → `XDebugger` в плагине.
Приложение со встроенным языком получает `DebugSession` напрямую, без DAP.

## Профилирование

Сейчас есть только время стадий (`LEX`, `PARSE`, `EXECUTE`, `SHUTDOWN`) — это ответ
на «сколько шёл скрипт», а не на «где он стоял». Нужны два режима, и они отвечают
на разные вопросы.

### Сэмплирование — основной режим

Цепочка `Frame` — неизменяемые звенья со ссылкой на родителя, то есть **готовое дерево
вызовов, которое можно безопасно читать из другого потока**. Не хватает одного:
опубликовать «текущий кадр» на поток.

* в `Run` — реестр «поток → слот» (`ConcurrentHashMap` + объект со `volatile Frame`);
  главный поток регистрируется при старте запуска, потоки скрипта — в `RunThreads`,
  который их и так создаёт;
* на входе в вызов и на выходе — одна запись в слот. **Без `nanoTime`**: пара замеров
  времени стоит порядка двадцати наносекунд на вызов, запись в поле — единицы;
* отдельный поток снимает слоты N раз в секунду и складывает цепочки в дерево.

Получается честный профиль по функциям с местами вызова из `callSite`. Гранулярность
по строкам — тем же слотом из хука отладчика (`beforeStatement` пишет `span`),
включается отдельно: платит тот, кто попросил.

Важная оговорка, которую надо написать в отчёте, а не оставлять читателю: сумма
сэмплов не равна времени по часам — по той же причине, по которой сумма замеров стадий
ему не равна (`docs/metrics.md`).

### Счётчики вызовов — второй режим

Сэмплер не отвечает на «сколько раз позвали» и «сколько стоит один вызов». На это
отвечает инструментирование в `UserFunction.call`: счётчик вызовов, суммарное
и собственное время. Дороже сэмплера, поэтому отдельным ключом.

### Что мерить кроме времени

* максимальную достигнутую глубину вызовов (предел — 256, полезно видеть запас);
* попадания и промахи таблицы членов;
* число созданных значений по типам — первый ответ на «почему скрипт ест память».

Вывод — рядом с `--metrics`, плюс `--profile-out` в JSON: то же дерево читают
и плагин, и внешние смотрелки flame graph.

## Плагин IntelliJ: состав

Отдельная сборка Gradle в `ide/idea-plugin/` со своим `settings.gradle.kts`, который
подключает корневой репозиторий через `includeBuild("../..")`. Так `./gradlew build`
в корне остаётся быстрым и чистым — иначе он начнёт качать дистрибутив IDE, — а
инвариант «репозитории только в `settings.gradle.kts`» соблюдается сам собой:
`intellijPlatform { defaultRepositories() }` объявляется в настройках той сборки.
JDK 21 совпадает с требованием IDEA 2024.2+, отдельного тулчейна не надо.

| Точка расширения | Что даёт | Опирается на |
|---|---|---|
| `fileType` + `lang.parserDefinition` | язык, `.wdl`, PSI | лексер с тривией, «проигрывание» AST |
| `lang.syntaxHighlighterFactory`, `colorSettingsPage` | подсветка и её настройка | виды токенов |
| `lang.braceMatcher`, `lang.commenter`, `lang.foldingBuilder` | скобки, `Ctrl+/`, свёртка | токены и интервалы узлов |
| `annotator` | подчёркивание ошибок, подсветка имён по видам | `Diagnostics`, `ScopeTree` |
| `completion.contributor` | имена, члены после точки, ключевые слова, пути модулей | `visibleAt`, `BuiltinMembers`, `Catalog` |
| `gotoDeclarationHandler`, `lang.findUsagesProvider` | переход и поиск употреблений | `resolve`, `usages` |
| `lang.documentationProvider` | подсказка по наведению | `Comments`, `Signature` |
| `lang.psiStructureViewFactory` | структура файла | обход дерева |
| `localInspection` | «имя не используется», «объявлено ниже», «снимок в условии цикла» | анализатор, `Member.snapshot()` |
| `configurationType`, `runConfigurationProducer` | запуск файла, ключи `--metrics`, `--tokens`, `--ast`, `--profile` | CLI как есть |
| `consoleFilterProvider` | `файл:строка:столбец` в консоли — ссылка | формат `Diagnostics.render` |
| `XDebuggerSupport` | точки останова, кадры, переменные, вычисление | `DebugSession` |

Текущие `setting ide/idea/filetypes/wdlang.xml` и внешний инструмент из
`setting ide/idea/tools/` после появления подсветки и конфигурации запуска становятся
не нужны и удаляются: плагин (или LSP-клиент) делает то же самое и правильнее.

## Редактор внутри приложения

Случай, ради которого смысловой слой и вынесен наружу: приложение встроило язык
и даёт пользователю писать скрипты у себя (`sys.gui` — Swing, так что редактор живёт
в том же процессе и в том же окне).

Что оно берёт и в каком виде:

* **подсветка** — `Lexer` в режиме с тривией: список `(Span, TokenType)` на текст,
  перелексация куска дёшева, состояния между токенами нет;
* **ошибки** — `Diagnostics.all()`, интервалы прямо в подчёркивание;
* **дополнение** — `visibleAt(offset)` и `BuiltinMembers`, плюс свой `Catalog`:
  приложение объявляет собственные классы и модули через `wdl-bridge` и **само же
  описывает их каталогом** — тот же принцип, что и в `wdl-stdlib`;
* **переход и наведение** — `resolve(offset)`, `Comments`;
* **запуск и отладка** — `WdlEngine`/`WdlScript` и `DebugSession` напрямую,
  без DAP и без второго процесса.

Никакого LSP здесь не нужно и заводить его не надо: сериализация имеет смысл только
на границе процессов. Требование к слою анализа от этого одно, и его надо держать
с первого дня: **ни одного класса из `lsp4j` или IntelliJ в `wdl-tools`**.

## Краткая дорожная карта

| Этап | Содержание | Главный результат |
|---|---|---|
| 0 | Контракты позиций и инварианты AST | дерево безопасно адресуется |
| 1 | Lossless lexer | полная подсветка и комментарии |
| 2 | `Fragment`, `Nodes`, `nameSpan` | единый навигационный AST API |
| 3 | Протокол-независимый language service | diagnostics/symbols/completion через Java |
| ~~4~~ | ✔ Каталоги builtins, stdlib и host API | editor знает конфигурацию движка |
| 5 | Best-effort types и context completion | полезное дополнение членов |
| ~~6~~ | ✔ Отдельный `wdl-lsp` | поддержка разных редакторов |
| 7 | Formatter, rename, inspections | безопасное редактирование |
| ~~8~~ | ✔ Минимальный IntelliJ-плагин | основной рабочий сценарий в IDEA |
| 9 | Протокол-независимая `DebugSession` | breakpoints и steps через Java API |
| 10 | Отдельный `wdl-dap` | стандартная внешняя отладка |
| 11 | IntelliJ `XDebugger` bridge | нативное окно Debug |
| 12 | Sampling/exact profiler | flame graph и счётчики |
| 13 | `WdlToolingSession` | полноценный встроенный редактор |

Порядок 0 → 6 лучше не переставлять: это единый фундамент редакторной модели.
Этапы 7 и 8 после стабильного LSP можно выполнять параллельно. Отладочная ветка
9 → 11 опирается на позиции и snapshots, но не обязана ждать formatter. Профилировщик
можно начинать после появления structured frames на этапе 9.

## Подробная последовательность реализации

> **Этапы 0, 1 и 2 закрыты 5 сентября 2026 года.** Что именно сделано и какие решения
> принимались по дороге — `plans/ПланПодготовкиКLSP.md`; правила для инструментов
> собраны в `docs/tooling.md`. Дальнейшая работа начинается с этапа 3 — слоя анализа
> имён в `wdl-tools`.

Ниже — исполняемый порядок работ. Каждый этап заканчивается проверяемым шлюзом.
Если шлюз не выполнен, следующий этап не начинается: иначе ошибки нижнего слоя
превратятся в обходы и дублирование в каждом редакторе.

### Этап 0. Зафиксировать контракты исходного текста и AST

**Цель:** сделать смещения и структуру дерева надёжным публичным контрактом.

Работы:

1. Ввести тестовую утилиту `AstInvariant`.
2. Для каждого `Node` рекурсивно проверять:
   - `span != null`;
   - `0 <= start <= end <= source.length` либо `Span.NONE` только для явно
     разрешённых синтетических узлов;
   - интервал ребёнка вложен в интервал родителя;
   - дети отсортированы слева направо;
   - независимые дети не пересекаются;
   - `Program.span` покрывает документ.
3. Исправить `Parser.access()` для незаконченного `obj.`: ставить нулевой
   `ErrorExpr` сразу после точки и не присваивать ему следующий токен.
4. Прогнать инварианты по всем `examples/**/*.wdl`.
5. Добавить отдельный корпус незаконченного текста: пустой файл, `obj.`,
   незакрытые строки/комментарии/скобки, неполные `def`/`class`/`trait`/`import`
   и несколько независимых ошибок.
6. Зафиксировать кодировку позиций: `Span` — UTF-16 offset, позиции сервиса — с нуля,
   CLI-позиции — с единицы.
7. Дополнить `Source` методом `offsetOf(line, character)` и тестами LF, CRLF,
   кириллицы, surrogate pairs, пустой последней строки и BOM.

**Шлюз этапа:**

- malformed-корпус не нарушает инварианты;
- `offset → position → offset` обратимо;
- runtime-тесты зелёные;
- новых зависимостей в `wdl-core` нет.

### Этап 1. Сделать лексер пригодным для редактора

**Цель:** получить lossless-поток, покрывающий каждый символ документа.

Работы:

1. Добавить `WHITESPACE`, `LINE_COMMENT`, `BLOCK_COMMENT`, `BAD_CHARACTER`.
2. Добавить `Kind.TRIVIA` и методы `isTrivia()`/`isBad()`.
3. Ввести `LexerMode.RUNTIME` и `LexerMode.LOSSLESS`.
4. Не заставлять runtime-парсер платить за trivia.
5. Для `BAD_CHARACTER` сохранять токен и диагностику.
6. Для незакрытого комментария возвращать токен до конца документа.
7. Проверять `concatenate(token.sourceSlices()) == source.text()`.
8. Измерить лексирование 1, 10 и 100 тысяч строк; потоковый API добавлять только
   при подтверждённой проблеме.

**Шлюз этапа:**

- lossless-токены идут без дыр и пересечений;
- runtime-поток не изменился;
- комментарии и плохие символы доступны подсветке.

### Этап 2. Завершить адресуемость AST

**Цель:** любой значимый фрагмент находится по смещению и имеет точный интервал имени.

Работы:

1. Добавить недостающие `nameSpan`; `FunctionExpr.Param.span` не менять.
2. Ввести `Fragment extends Node` для параметров, аргументов, декораторов,
   записей объекта, обработчиков, привязок `use`, свойств и ссылок на типы.
3. Создать `Nodes.children`, `walk`, `deepestAt`, `pathAt`,
   `declarationNameAt`.
4. Закрепить правила полуинтервалов и нулевых error nodes.
5. Перевести инструменты на общий обход без изменения runtime-семантики.
6. Проверять, что каждый ребёнок посещён ровно один раз.

**Шлюз этапа:**

- IDE-инструментам не нужен свой полный switch по AST;
- адресуются все имена;
- `pathAt` стабилен на корректном и битом коде.

### Этап 3. Ввести протокол-независимый языковой сервис

**Цель:** отвечать редактору обычным Java API без знания про LSP или IntelliJ.

~~~java
public interface LanguageService {
    DocumentSnapshot open(DocumentId id, long version, String text);
    DocumentSnapshot change(DocumentId id, long version, String text);
    void close(DocumentId id);
    List<Completion> complete(DocumentId id, TextPosition position);
    Optional<Hover> hover(DocumentId id, TextPosition position);
    Optional<Location> definition(DocumentId id, TextPosition position);
    List<Location> references(DocumentId id, TextPosition position);
    List<DocumentSymbol> symbols(DocumentId id);
}
~~~

Работы:

1. Создать versioned `DocumentStore`.
2. Хранить overlay открытых документов поверх диска.
3. Сделать `DocumentSnapshot` неизменяемым.
4. Не публиковать версию N после прихода N+1.
5. Добавить cancellation token.
6. Построить `ScopeTree` всех видов областей.
7. Создать `Symbol` со стабильным id, видом, spans, контейнером, сигнатурой,
   документацией и происхождением.
8. Реализовать `visibleAt` по runtime-правилам.
9. Построить reference → symbol.
10. Реализовать `resolve` и `usages` сначала в файле, затем в workspace.
11. Привязать комментарии к объявлениям.
12. Никогда не выполнять пользовательский код ради анализа.

**Совместные тесты:** рекурсия, объявление ниже, shadowing, imports, параметры,
остатки, `catch`, `use`, циклы, классы, свойства, фабрики, traits, `extend`
и битое дерево. Один fixture должен анализироваться и исполняться.

**Шлюз этапа:** консольный Java-клиент получает diagnostics, symbols, completion
и definition; в analysis нет классов LSP4J/IntelliJ.


> **Этап 3 (слой анализа имён) закрыт 5 сентября 2026 года** — смысловая часть:
> `FileAnalysis`, дерево областей, символы, `visibleAt`, `resolve`, `usages`,
> комментарии. Подробности и решения — `plans/ПланАнализаИмён.md`. `DocumentStore`
> с версиями, интерфейс `LanguageService` и cancellation остаются серверу (этап 6).

### Этап 4. Описать builtins, stdlib и возможности приложения

> **Закрыт 6 сентября 2026 года.** Что сделано и какие решения принимались —
> `plans/ПланКаталогаИмён.md`; правила — раздел «Каталог внешних имён»
> в `docs/tooling.md`. Против плана ниже изменилось одно, зато главное:
> **каталог снимается с движка, а не пишется рядом с ним** — рукописных
> `Std.catalog()` и `Sys.catalog()` не появилось, а вместе с ними отпал и пункт 10
> («сравнивать runtime exports с каталогом»): сравнивать нечего, источник один.
> Руками объявляется только описание словами — `.doc(...)` в построителе.
> Из невыполненного остался `wdl.json` (пункт 9): конфигурация принадлежит
> серверу, до него читать её некому.

**Цель:** редактор знает ровно ту конфигурацию языка, которую собрал хозяин движка.

~~~java
public interface Catalog {
    Collection<SymbolDescriptor> roots();
    Optional<ModuleDescriptor> module(String key);
    Collection<MemberDescriptor> members(TypeDescriptor type);
}
~~~

Работы:

1. Описать builtins и type descriptors.
2. Дать безопасный API чтения встроенных членов.
3. Расширить `Module.Builder`: runtime-регистрация и metadata создаются вместе.
4. Записывать документацию, параметры, arity, type hint и side-effect flag.
5. Для `install(...)` добавить явный `describe(...)`.
6. Связать каталог с `WdlEngine.Builder`.
7. Дать `define` необязательный descriptor.
8. Реализовать `Std.catalog()` и `Sys.catalog()`.
9. Добавить `wdl.json`: source roots, stdlib, catalogs, modules.
10. Тестом сравнивать runtime exports с каталогом.

**Шлюз этапа:** стандартные и host-имена видны completion; одно изменение модуля
обновляет runtime и editor metadata.

### Этап 5. Добавить best-effort типы и полноценное completion

**Цель:** полезное дополнение членов без ложной статической строгости.

Типы: `Unknown`, `ValueType`, `ClassType`, `TraitType`, `ModuleType`,
`FunctionType`, `UnionType` и `Error` для восстановления анализа.

Источники: литералы, `new`, коллекции, каталог, imports, host metadata,
однозначное присваивание и простое expression-body.

Контексты completion: начало инструкции, выражение, после точки, именованный
аргумент, import path, type position, тело типа, оператор в обратных кавычках.

Каждый ответ несёт kind, label, insert text/snippet, signature, documentation,
sort key и дополнительные import edits.

**Шлюз этапа:** `x.` работает на недописанном файле; известный тип не получает
чужих членов; `Unknown` не создаёт ложную ошибку; signature help понимает named
и variadic arguments.

### Этап 6. Реализовать LSP-сервер

> **Закрыт 6 сентября 2026 года** вместе с этапом 8. Что сделано и какие решения
> принимались — `plans/ПланЯзыковогоСервера.md`; правила — разделы «Языковой сервис»
> и «Языковой сервер» в `docs/tooling.md`. Против плана ниже изменилось одно:
> **документы, версии и ответы живут в `wdl-tools`, а не в сервере** (пакет
> `ru.wds.wdl.tools.service`) — сервер оказался чистым переводом в JSON-RPC, и это
> ровно то, ради чего слой выносили: плагин IDEA и встроенный редактор берут те же
> ответы напрямую. Из списка методов сделаны 1–8; 9–12 (`foldingRange`,
> `workspaceSymbol`, `rename`, форматирование, `codeAction`) не сделаны и **не
> объявлены**: возможность объявляется только после реализации. Отмены запросов нет
> по той же причине, по какой её не было в наброске этапа 3 — её незачем заводить
> раньше первой жалобы на задержку.

**Цель:** открыть один сервис нескольким редакторам.

Порядок методов:

1. lifecycle;
2. didOpen/didChange/didClose;
3. diagnostics;
4. documentSymbol;
5. completion/resolve;
6. hover и signatureHelp;
7. definition/references;
8. semanticTokens;
9. foldingRange/workspaceSymbol;
10. prepareRename/rename;
11. formatting/rangeFormatting;
12. codeAction.

Правила: stdio stdout только JSON-RPC; логи в stderr; `positionEncoding=utf-16`;
монотонные версии; stable diagnostic codes; cancellation; отдельный кэш на root URI;
capability объявляется только после реализации.

Тесты: DTO mapping, настоящий JSON-RPC, golden capabilities, гонка версий,
cancel, overlay→disk после close, Unicode/CRLF и smoke в нескольких клиентах.

**Шлюз этапа:** один fixture даёт эквивалентные ответы direct API и LSP; сервер
корректно завершается и не оставляет потоков.

### Этап 7. Formatter, rename и инспекции

Formatter обязан сохранять комментарии, быть идемпотентным, не менять AST
корректной программы и возвращать минимальные edits.

Rename обязан учитывать shadowing и конфликты, не трогать недоказанные строковые
обращения и только возвращать `WorkspaceEdit`.

Первые inspections: unused local, unreachable statement, guaranteed unknown name,
duplicate declaration, known signature mismatch, unresolved import, snapshot member
in loop condition и мягкое shadowing builtin.

**Шлюз этапа:** formatter проходит round-trip/idempotence; rename проверен на
нескольких файлах; quick fixes привязаны к diagnostic code.

### Этап 8. Минимальный IntelliJ-плагин

> **Закрыт 6 сентября 2026 года.** `ide/idea-plugin/` — отдельная сборка, четыре
> класса и `plugin.xml`: тип файла и `LspServerSupportProvider`, запускающий
> `wdl-lsp --stdio`. Сервер плагин внутри себя **не носит** — он часть дистрибутива
> wdl и обязан быть той же версии, что и движок; ищется по `wdl.lsp.home`,
> `WDL_LSP_HOME`, сборке внутри проекта и `PATH`. Пункты 4–6 (конфигурация запуска,
> фильтр консоли, Plugin Verifier) остались на потом: запуск скрипта уже делает
> внешний инструмент из `run-wdl.bat`, и заводить конфигурацию имеет смысл вместе
> с отладчиком. Этап 7 (форматтер, rename, инспекции) сознательно пропущен вперёд
> ради работающего редактора — он от этого не зависит.

**Цель:** рабочий сценарий в IDEA без дублирования семантики.

1. `Language`/`FileType`, icon, commenter, brace matcher, syntax highlighting.
2. Запуск `wdl-lsp --stdio` публичным API целевой IDEA.
3. Настройка Java/дистрибутива.
4. Run Configuration: script, working dir, args, roots, stdlib, environment.
5. Console filter.
6. Plugin Verifier.
7. PSI-проекцию делать только если LSP реально не покрывает нужные функции.

**Шлюз этапа:** подсветка, diagnostics, completion, navigation и Run работают;
сервер закрывается с проектом; два проекта изолированы.

### Этап 9. Протокол-независимая отладка

**Цель:** остановить выполнение и читать состояние без знания о DAP.

Работы:

1. `ExecutionInstrumentation.off()` на уровне `Run`.
2. `beforeStatement` для всех `Stmt`, включая верхнеуровневый `ExprStmt`.
3. `functionEntered/functionExited` в `UserFunction`.
4. `exceptionThrown` до форматирования ошибки.
5. Read-only `DebugContextView`.
6. Structured frames и scope snapshots.
7. Стабильные thread ids.
8. Resolver line breakpoint → executable span.
9. Suspend policy `THREAD`, затем `ALL` с rendezvous/timeout.
10. Не держать runtime locks во время suspension.
11. Инвалидировать handles после resume.
12. Evaluate/conditional breakpoints добавлять последними.

Шаги определяются глубиной: in — любая; over — `depth <= initial`; out —
`depth < initial`.

**Шлюз этапа:** unit-тест без DAP получает locals; шаги проходят рекурсию, циклы,
exceptions и modules; несколько потоков останавливаются независимо; overhead измерен.

### Этап 10. Реализовать DAP

Первая версия: initialize, launch, breakpoints, exception breakpoints,
configurationDone, threads, stackTrace, scopes, variables, continue/next/step,
pause, disconnect/terminate и стандартные события.

Требования: output скрипта идёт DAP event; handles живут до resume; значения
раскрываются лениво и с пагинацией; path/URI переносимы; неподдерживаемые
capabilities не рекламируются; launch и attach не смешиваются.

**Шлюз этапа:** интеграционный клиент и VS Code проходят один сценарий; debugging
модуля работает; terminate закрывает instance, threads и libraries.

### Этап 11. Подключить XDebugger IntelliJ

Зарегистрировать line breakpoints и debug runner, реализовать `XDebugProcess`,
threads/frames/scopes/lazy variables, команды шага, фактические позиции breakpoint,
console output и watches/evaluate после MVP.

**Шлюз этапа:** breakpoint переживает перезапуск IDE; execution point движется;
locals не блокируют UI; Debug использует тот же catalog, что Run и LSP.

### Этап 12. Реализовать профилировщик

Основной режим — sampling логических WDL frames через thread slots. Дополнительный —
exact call counters: calls, inclusive/self time, exceptions, max depth. Отдельно
различать wall time и thread CPU time.

Форматы: CLI table, Speedscope JSON; опционально Chrome Trace и JFR custom events.

Профиль не помещать в LSP/DAP: его читают `ProfileSession` API или artifact.

**Шлюз этапа:** выключенный profiler не создаёт поток; multi-thread профиль корректен;
overhead измерен; artifact открывается Speedscope viewer.

### Этап 13. Подключить собственный редактор

Создать `WdlToolingSession`, объединяющий engine, catalog и editor buffers.
Он отдаёт tokens, diagnostics, semantic operations, formatting edits и
Run/Debug/Profile sessions, имеет cancellation и `close()`.

Swing/JavaFX используют API напрямую; Monaco/CodeMirror — LSP через WebSocket
или gateway.

**Шлюз этапа:** fixture эквивалентен direct API и LSP; host catalog виден в edit/run;
закрытие освобождает executors/watchers/sessions.

## Матрица обязательных проверок перед стабильным релизом

| Область | Минимальная проверка |
|---|---|
| Core | `./gradlew build` и запрет runtime-зависимостей |
| AST | инварианты на examples и malformed corpus |
| Analysis | runtime/analysis differential tests |
| Documents | версии, cancellation, unsaved overlay |
| LSP | protocol tests и smoke в трёх клиентах |
| Formatter | round-trip и идемпотентность |
| Rename | конфликты, shadowing, workspace |
| Debug | steps, exceptions, modules, threads |
| DAP | lifecycle, handles, output isolation, termination |
| Profile | overhead, threads, export |
| IntelliJ | Plugin Verifier и UI smoke |
| Embedded | direct API, resource leak checks |

## Определение готовности

- **0.1, editor MVP:** lossless lexer, diagnostics, symbols, completion, hover,
  definition и один LSP-клиент.
- **0.2, language service:** workspace imports, references, rename, formatter,
  catalogs и стабильный embedded API.
- **0.3, debugger:** DebugSession, DAP, breakpoints, threads, stack, locals,
  step in/over/out и exception breakpoints.
- **0.4, performance:** sampling, call counters, Speedscope export.
- **1.0, platform:** стабильные API, общие fixtures direct/LSP/DAP, опубликованный
  IntelliJ-плагин, документация host catalogs, единая конфигурация edit/run/debug/profile.

## Риски и открытые вопросы

* **Дублирование правил видимости.** Анализатор повторяет то, что делает `Scope`
  во время выполнения. Расхождение будет проявляться как «редактор предложил имя,
  а скрипт упал». Лечится не архитектурой, а тестами на общих файлах: один и тот же
  `.wdl` проверяется и запуском, и анализатором.
* **Политика останова на языке без GIL.** Проектное решение, а не деталь реализации;
  «остановить всё» на честном многопоточном запуске требует точки сбора и таймаута.
* **Стоимость runtime-хуков.** Основной хук находится в `visit`, специальный вызов —
  у верхнеуровневого `ExprStmt`, вход/выход функции — в `UserFunction`. Все эти
  места надо мерить до и после, иначе через полгода никто не вспомнит их цену.
* **«Проигрывание» AST в PSI.** Приём рабочий, но требует строгой вложенности
  интервалов — этап 0 существует ровно поэтому. Запасной путь — плоское PSI (файл,
  а под ним листья-токены) плюс собственные ссылки на именах: теряются готовые
  рефакторинги платформы, остальное работает.
* **Тип получателя после точки.** Язык динамический, честного вывода типов не будет.
  Планка сознательно низкая: точно там, где видно из текста, иначе — все члены всех
  типов.
* **Стоимость подсветки.** Обёртка лексирует переданный кусок заново, собирая список.
  Для скриптов в тысячи строк это незаметно; если станет заметно — потоковый `next()`,
  но не раньше, чем это подтвердится замером.
* **Версии платформенных API.** Клиенты LSP и DAP в IntelliJ молодые, `sinceBuild`
  стоит держать на актуальной ветке (2024.2+): поддержка старых сборок — отдельная
  работа, а пользователь пока один.

## Основные спецификации и документация

- [Language Server Protocol](https://microsoft.github.io/language-server-protocol/) —
  актуальная спецификация LSP и список реализаций.
- [Debug Adapter Protocol](https://microsoft.github.io/debug-adapter-protocol/) —
  lifecycle, capabilities, breakpoints, threads, frames, scopes и variables.
- [LSP в IntelliJ Platform](https://plugins.jetbrains.com/docs/intellij/language-server-protocol.html) —
  поддержанные методы, ограничения продуктов и актуальные extension points.
- [Custom Language Support в IntelliJ Platform](https://plugins.jetbrains.com/docs/intellij/custom-language-support.html) —
  нативные PSI, completion, navigation, formatter и refactorings.
- [Speedscope file format](https://github.com/jlfwong/speedscope/blob/main/file-format-schema.json) —
  переносимый формат профиля и flame graph.

Версии библиотек и целевой IntelliJ build не следует фиксировать этим документом
навсегда. Их выбирают в начале соответствующего этапа, записывают в version catalog
и покрывают compatibility/integration tests.

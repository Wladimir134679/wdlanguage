# Документация wdl

Здесь описан сам язык: что он такое, из чего состоит и почему устроен так, а не иначе.
Обоснования важны не меньше правил — язык ещё растёт, и через полгода нужно будет
понимать, какое решение можно менять, а какое держит на себе остальные.

## Оглавление

| Документ | О чём |
|---|---|
| [Идея языка](overview.md) | зачем wdl нужен, что обещает автору скрипта и приложению, как устроен конвейер |
| [Типы данных](types.md) | одиннадцать типов, их правила и внутреннее устройство, истинность, равенство, печать |
| [Выражения](expressions.md) | приоритеты, семантика операций (включая `in`, `has` и диапазон `..`), перегрузка операторов классом, рецепты «как добавить оператор / тип / узел» |
| [Инструкции](statements.md) | присваивание, распаковка `x, y = *point` и `host, port, **rest = **config`, пропуск `_`, вызов, встроенные функции, вывод, границы инструкций |
| [Ветвления и циклы](control-flow.md) | `if`, `while`, `for`, перебор `for ... in` и с ключом `for (i, tile in row)`, `match` и `yield`, `break`/`continue`, области видимости |
| [Функции](functions.md) | объявление, `=>`, `return`, анонимные, замыкания, области видимости вызова |
| [Единое обращение](access.md) | почему `a.b` и `a["b"]` — одна операция, и что из этого следует |
| [Члены значений](members.md) | `text.size`, `a.sort()`, интроспекция, `extend`, оператор как член, наборы по типам |
| [Классы](classes.md) | `class` и `trait`, `new`, `this` и `super`, наследование, требования трейтов, `is` |
| [Ошибки и ресурсы](errors.md) | иерархия `Exception`, `throw`, `try`/`catch`/`finally`, `try?`/`try!`, `defer`, `use` и `Closeable`, трассировка |
| [Декораторы](decorators.md) | `@[...]`, метаданные, порядок применения, `like`, обёртка для класса |
| [Аннотации](annotations.md) | `@{...}`, данные при объявлении, чтение членом `annotations`, чем отличаются от декоратора |
| [Модули](modules.md) | `import` и `import ... as`, пути, область действия импорта, наследование через файлы |
| [Потоки](threads.md) | модель памяти, `synchronized`, `sys.thread`: потоки, пул, замок, счётчик, канал, защёлка |
| [Лимиты выполнения](limits.md) | шаги, таймаут, квота потоков и вложенность: чем запуск отвечает на «а если скрипт чужой» |
| [Метрики](metrics.md) | время стадий: `--metrics`, отчёт из приложения, что меряется и что нет |
| [Профилировщик](profiling.md) | горячие функции: `--profile`, «всего» и «сам», граф вызовов, выгрузка в JSON для плагина IDE |
| [Отладчик](debugging.md) | остановка на строке, кадры и переменные, шаги, вычисление в кадре, останов на ошибке, чего отладчик не может, адаптер `wdl-dap` и отладка из редактора |
| [Встраивание](embedding.md) | как приложение добавляет в язык свои функции, классы и библиотеки |
| [Мост в Java](java-interop.md) | как отдать скрипту чужой класс и готовый Java-объект через рефлексию |
| [Игра со встроенными скриптами](game.md) | пинг-понг на Java, правила которого написаны на wdl: пять хуков, спрайты, холст — площадка, на которой проверяется встраивание |
| [Инструментам](tooling.md) | позиции и их единицы, режим лексера с комментариями, обход дерева `Nodes`, анализ имён (`FileAnalysis`), каталог внешних имён и языковой сервис (`LanguageService`) — то, на чём стоят редактор, `wdl-lsp` и плагин IDEA |

Черновые заметки автора о том, каким язык хочется видеть, лежат отдельно —
в `Основные идеи.md` в корне репозитория. Это источник желаний, а не описание
реализованного: то, что уже работает, описано здесь.

## С чего начать

Читать по порядку: [идея](overview.md) → [типы](types.md) → [выражения](expressions.md)
→ [инструкции](statements.md) → [ветвления и циклы](control-flow.md) →
[функции](functions.md). Документ про [обращение](access.md) — отдельно, потому что
это решение уровня языка, а не деталь синтаксиса.

Пощупать руками:

```bash
./gradlew :wdl-cli:installDist              # → wdl-cli/build/install/wdl/bin/wdl

wdl examples/language/hello.wdl                      # программа: переменные и вывод
wdl examples/language/expressions.wdl                # шпаргалка по выражениям
wdl examples/language/control-flow.wdl               # ветвления и циклы
wdl examples/language/match.wdl                      # match: ветвление по одному предмету
wdl examples/language/const.wdl                      # константы: const NAME = value
wdl examples/language/functions.wdl                  # функции, замыкания, области видимости
wdl examples/classes/members.wdl                    # члены значений: свойства и методы у массива, строки, числа
wdl examples/classes/introspection.wdl              # что значение рассказывает о себе: параметры, состав класса
wdl examples/classes/extend.wdl                     # extend: свои члены у типа и у класса
wdl examples/classes/classes.wdl                    # классы: поля, методы, конструктор, фабрики
wdl examples/classes/inheritance.wdl                # наследование, super, полиморфизм
wdl examples/classes/traits.wdl                     # трейты: контракты и готовые реализации
wdl examples/classes/class-varargs.wdl              # остаток в заголовке класса: *args и **named
wdl examples/metaprogramming/decorators.wdl                 # декораторы: @[...], метаданные, обёртки
wdl examples/errors/errors.wdl                     # ошибки: throw, try/catch/finally, трейс
wdl examples/errors/resources.wdl                  # ресурсы: defer, use, трейт Closeable
wdl examples/testing/errors.wdl             # тесты на самом языке: проверки по ошибкам
wdl examples/modules/plain.wdl              # модули: import и import ... as
wdl examples/modules/late.wdl               # плагин загружается по требованию
wdl examples/concurrency/threads.wdl                    # потоки: spawn, join, interrupt, synchronized
wdl examples/concurrency/threads_pool.wdl               # пул, обещания, канал, защёлка
wdl examples/embedding/stdlib.wdl                     # стандартная библиотека: pow, File, Random
wdl examples/tooling/debugging.wdl                  # скрипт, по которому удобно шагать отладчиком
wdl --metrics examples/modules/plain.wdl    # время стадий после выполнения
wdl --metrics-each examples/concurrency/threads.wdl     # строка на каждую законченную стадию
wdl --ast examples/language/hello.wdl                # дерево программы
wdl --tokens examples/lexer/lexer-check.wdl       # поток токенов
./gradlew :wdl-cli:repl --console=plain     # построчно, интерактивно
```

## Как вести эти документы дальше

* **Один документ — одна тема.** Новая крупная возможность (функции, классы, модули)
  получает свой файл и строку в этой таблице.
* **Записывать не только правило, но и причину.** «Деление на ноль — ошибка» стоит
  дёшево; ценно то, почему не `inf`.
* **Примеры должны выполняться.** Всё, что приведено в документах, проверено запуском;
  при изменении поведения примеры и тексты ошибок обновляются вместе с кодом.
  Документ-проект — исключение, и оно помечается прямо в шапке документа и в таблице
  выше: описанного там ядро ещё не умеет, и примеры не запускаются. Пометка снимается
  вместе с реализацией — так было с [классами](classes.md).
* **Состояние языка — в одном месте.** Что уже работает и чего ещё нет, написано
  в [идее языка](overview.md#что-уже-работает) и в корневом `README.md`; в остальных
  документах этого дублировать не нужно.

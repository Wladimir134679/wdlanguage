/**
 * Анализ имён без выполнения: что объявлено в файле, где, и что видно в этой точке.
 * <p>
 * Слой существует потому, что в самом языке такой стадии нет и не будет: имя ищется
 * в момент выполнения, а {@code Resolver} между разбором и запуском был убран
 * сознательно. Редактору же нужно знать про имена до запуска — иначе не бывает
 * ни перехода к объявлению, ни дополнения, ни переименования.
 * <p>
 * Точка входа — {@link ru.wds.wdl.tools.analysis.FileAnalysis}. Модуль зависит только
 * от ядра и ничего не знает ни про LSP, ни про IDE: тот же результат одинаково годится
 * серверу, плагину, REPL и приложению со своим редактором.
 */
package ru.wds.wdl.tools.analysis;

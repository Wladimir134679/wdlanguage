/**
 * Языковой сервис: открытые документы и ответы редактору.
 * <p>
 * Слой между анализом и протоколом. {@link ru.wds.wdl.tools.analysis.FileAnalysis}
 * знает про один разобранный текст и ничего не помнит; редактору же нужно, чтобы
 * кто-то держал открытые документы, следил за версиями правок и складывал ответ
 * из файла и {@linkplain ru.wds.wdl.tools.catalog.Catalog каталога}. Это и есть
 * {@link ru.wds.wdl.tools.service.LanguageService}.
 * <p>
 * Про LSP здесь нет ни слова — и это требование, а не совпадение: тем же API
 * пользуются плагин IDEA и редактор внутри приложения, встроившего язык.
 * Перевод в JSON-RPC живёт в отдельном модуле {@code wdl-lsp}.
 */
package ru.wds.wdl.tools.service;

/**
 * Языковой сервер: перевод ответов {@link ru.wds.wdl.tools.service.LanguageService}
 * в JSON-RPC.
 * <p>
 * Правил языка здесь нет ни одной строки — и это требование к модулю, а не
 * наблюдение. Тем же слоем анализа пользуются плагин IntelliJ IDEA и редактор
 * внутри приложения, встроившего язык; всё, что окажется записано здесь, для них
 * потеряется.
 * <p>
 * Обратное правило тоже держится: классов lsp4j нет нигде, кроме этого модуля,
 * и проверяет это компилятор — в {@code wdl-tools} библиотеки нет на пути классов.
 */
package ru.wds.wdl.lsp;

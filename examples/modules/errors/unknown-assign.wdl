// Ожидаемая ошибка: в модуле '../lib/geometry' нет имени 'aria'.
// Присваивание модулю не заводит новых имён, в отличие от объекта: состав модуля
// задан его файлом, и опечатку надо назвать сразу — иначе рядом с настоящим именем
// молча появилось бы второе, похожее.

import "../lib/geometry" as g

g.aria = def(radius) => 0

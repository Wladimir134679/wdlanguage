// Ожидаемая ошибка: модуль 'lib/nothing' не найден.
// Ключ в сообщении — тот, по которому искали, а не тот, что написан:
// путь уже разрешён относительно каталога этого файла.

import lib.nothing

println("сюда выполнение не дойдёт")

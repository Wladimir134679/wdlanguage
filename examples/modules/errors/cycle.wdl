// Ожидаемая ошибка: циклический импорт: ring/a → ring/b → ring/a.
// Отдавать нечего — модуль ещё не выполнен, поэтому скрипт останавливается
// с цепочкой в сообщении, а не уходит в бесконечную загрузку.

import ring.a

println("сюда выполнение не дойдёт")

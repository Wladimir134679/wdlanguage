// Ожидаемая ошибка: в модуле 'lib/geometry' нет имени 'aria'.
// В отличие от объекта, модуль на несуществующее имя отвечает сразу: его состав
// задан файлом, и опечатку видно здесь, а не через два шага на вызове null.

// Путь через "..": файл лежит в errors/, а модуль — в соседней папке lib/.
import "../lib/geometry" as g

println(g.aria(2))

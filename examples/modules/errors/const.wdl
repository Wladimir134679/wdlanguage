// Ожидаемая ошибка: 'PI' нельзя присвоить: это константа.
// Модуль изменять можно, но const остаётся const — снаружи действует ровно
// то же правило, что внутри его файла.

import "../lib/geometry" as g

println("площадь пока считается по 3.14159: ", g.area(1))

g.PI = 3

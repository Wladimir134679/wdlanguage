// Ожидаемая ошибка: модуль 'lib/geometry' изменять нельзя.
// Модуль выполняется один раз за запуск, и значение у всех, кто его импортировал,
// общее: запись отсюда меняла бы чужой файл всем сразу.

import "../lib/geometry" as g

g.PI = 3

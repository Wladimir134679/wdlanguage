// Ожидаемая ошибка: неизвестный класс 'Shape'.
// Тип из модуля виден только ниже своего import — выше него модуля ещё нет,
// и наследоваться не от чего. Ошибка появляется на строке с class, когда до неё
// доходит выполнение: строка выше успевает отработать, и её видно в выводе.

println("эта строка успевает отработать")

class Circle(radius) : Shape("круг")

import "../lib/shapes"

println(new Circle(2).text())

// Ожидаемая ошибка: деление на ноль — с местом в lib/faulty.wdl.
// Это и есть проверка того, что ошибка знает свой файл: функция вызвана отсюда,
// но написана там, и показывать надо её строку.

import "../lib/faulty" as broken

println("до вызова")
println(broken.half(10))

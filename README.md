# wdl

Встраиваемый скриптовый язык для JVM. Динамическая типизация, всё — значение,
несколько взаимно изолированных интерпретаторов в одном процессе.

Собственные лексер и парсер, генераторы парсеров не используются.

## Модули

| Модуль | Назначение | Зависимости |
|---|---|---|
| `wdl-core` | лексер, парсер, AST, значения, интерпретатор | **нет, и не будет** |
| `wdl-bridge` | встраивание: свои классы, трейты и модули для скрипта плюс мост в Java (чужой класс и готовый объект как класс и объект языка) | `core` |
| `wdl-stdlib` | стандартная библиотека `std` и встроенные модули `sys.io`, `sys.json`, `sys.net.http` | `core`, `bridge` |
| `wdl-api` | фасад для встраивания: `WdlEngine`, компиляция, биндинг Java | `core`, `stdlib` |
| `wdl-tools` | дамп AST и токенов, анализ имён, каталог, языковой сервис для редакторов | `core` |
| `wdl-lsp` | языковой сервер LSP поверх `wdl-tools` | `tools`, `stdlib` |
| `wdl-dap` | адаптер отладки DAP поверх сессии ядра | `api`, `tools`, `stdlib` |
| `wdl-cli` | запуск скриптов из консоли, REPL | `api`, `tools` |
| `wdl-game` | WDGame: игровой движок на Swing/Graphics2D и пинг-понг на нём, правила которого написаны скриптами — площадка для проверки встраивания | `core`, `bridge`, `api` |

Отсутствие зависимостей у `wdl-core` — не пожелание, а проверяемое правило:
задача `checkNoRuntimeDependencies` встроена в `check` и роняет сборку при нарушении.

## Правила, на которых держится архитектура

1. **Никакой изменяемой статики в ядре.** Всё состояние — на экземпляре,
   контекст передаётся аргументом. Это то, что делает изоляцию интерпретаторов настоящей.
2. **У узлов AST нет `eval()`.** AST — данные, выполнение живёт в `runtime`.
   Скрипт компилируется один раз и запускается сколько угодно раз в разных окружениях.
3. **Одно пространство имён.** Функция — обычное значение в обычной переменной,
   иначе не будет ни замыканий, ни функций-полей.
4. **Обращение одно на все типы.** `значение.имя` — это в точности `значение["имя"]`:
   поле объекта, элемент массива, ключ словаря и поле класса — один механизм.
   См. [docs/access.md](docs/access.md).
5. **Тип языка и класс реализации — разные вещи.** Тип `object` представлен двумя
   классами: `MapValue` — карта пар, `InstanceObjectValue` — экземпляр класса, её
   наследник. Скрипт разницы не видит (`typeof` у обоих даёт `object`), а обычный
   объект не платит полями за то, что нужно только экземпляру. Правило общее: то,
   что нужно частному случаю, живёт в частном случае.
6. **`Span` в каждом токене и узле.** Позиции добавлять задним числом всегда дороже.
7. **Репозитории объявляются только в `settings.gradle.kts`.** Модуль не может
   втихую притащить себе зависимость.

## Документация

Язык описан в [`docs/`](docs/README.md):

* [Идея языка](docs/overview.md) — зачем wdl, что он обещает автору скрипта
  и встраивающему приложению, как устроен конвейер.
* [Типы данных](docs/types.md) — одиннадцать типов, их правила, внутреннее устройство
  и дескрипторы для проверки типа (`typeof`, `is`).
* [Выражения](docs/expressions.md) — приоритеты, семантика операций, перегрузка
  операторов классом, рецепты «как добавить оператор / тип / узел».
* [Инструкции](docs/statements.md) — присваивание, распаковка `x, y = *point`
  и `host, port, **rest = **config`, пропуск `_`, вызов, встроенные функции, вывод.
* [Ветвления и циклы](docs/control-flow.md) — `if`, `while`, `for`, перебор `for ... in`
  и с ключом `for (i, tile in row)`, `match`, `break`/`continue`, области видимости.
* [Функции](docs/functions.md) — объявление, `=>`, `return`, анонимные функции,
  значения параметров по умолчанию, именованные аргументы, вариативные параметры
  `*args`/`**named` и раскрытие `f(*array)`, замыкания, области видимости вызова.
  Раскрытие `*` и `**` работает и в литералах: `[*head, 3]`, `{**defaults, timeout: 60}` —
  см. [выражения](docs/expressions.md).
* [Единое обращение](docs/access.md) — почему точка и квадратные скобки это одна операция.
* [Члены значений](docs/members.md) — `text.size`, `a.sort()`, `213.toString()`,
  интроспекция функции и класса, путь в обход данных через дескриптор, `extend`.
* [Классы](docs/classes.md) — `class` и `trait`, `new`, `this` и `super`, наследование,
  свойства с `get`/`set` и скрытым полем, требования трейтов, проверка `is`,
  остаток в заголовке `*args`/`**named`.
* [Декораторы](docs/decorators.md) — `@[выражение](аргументы)`, метаданные первым
  аргументом, порядок применения, `like`, обёртка для класса через наследника.
* [Аннотации](docs/annotations.md) — `@{ключ: значение}` на объявлении, методе,
  свойстве и параметре, слияние блоков, чтение членом `annotations`, чем отличаются
  от декоратора.
* [Потоки](docs/threads.md) — модель памяти, `synchronized`, модуль `sys.thread`:
  потоки, пул, замок, счётчик, канал, защёлка.
* [Лимиты выполнения](docs/limits.md) — шаги, таймаут, квота потоков и вложенность:
  чем запуск отвечает на «а если скрипт чужой».
* [Метрики](docs/metrics.md) — время стадий конвейера: флаг `--metrics` в консоли,
  отчёт и логирование стадий из приложения, почему сумма замеров не равна времени
  по часам.
* [Профилировщик](docs/profiling.md) — какая функция горячая: флаг `--profile`
  в консоли, «всего» и «сам», граф вызовов, отчёт из приложения и выгрузка в JSON
  для плагина IDE.
* [Отладчик](docs/debugging.md) — остановка на строке, кадры со своими переменными,
  шаги `into`/`over`/`out`, вычисление выражения в кадре, останов на ошибке; чего
  отладчик не может и сколько стоит выключенный. Там же — адаптер `wdl-dap`
  и отладка из IntelliJ IDEA.
* [Встраивание](docs/embedding.md) — как приложение добавляет в язык свои функции,
  классы и библиотеки, и почему скрипт не видит разницы.
* [Игра со встроенными скриптами](docs/game.md) — пинг-понг на Java, правила
  которого написаны на wdl: пять хуков, четыре файла в ресурсах, порядок кадра,
  спрайты и холст; зачем игра живёт в этом репозитории и что именно она проверяет.
* [Инструментам](docs/tooling.md) — позиции и их единицы, режим лексера с комментариями,
  обход дерева `Nodes`, интервалы имён, инварианты интервалов, анализ имён
  (`FileAnalysis`) и каталог внешних имён (`Catalog`): то, на чём строятся подсветка,
  переход к объявлению, дополнение, форматтер и LSP.

## Команды

Для использования из любого каталога: `./gradlew installWdl` (Windows:
`.\gradlew.bat installWdl`). Для сборки с тестами и плагином IDEA:
`./gradlew prepareWdl`. После установки перезапустите терминал и IDEA.
Команды `wdl`, `wdl-lsp` и `wdl-dap` будут доступны через пользовательский `PATH`.

**[Установка, аргументы скриптов, запуск проектов и зелёная кнопка IDEA](docs/installation.md)**.

```bash
./gradlew build                  # сборка + тесты + проверка чистоты ядра
./gradlew modules                # карта модулей
./gradlew :wdl-cli:run --args="--help"
./gradlew :wdl-cli:run --args="examples/language/hello.wdl"                # выполнить скрипт
./gradlew :wdl-cli:run --args="examples/language/expressions.wdl"          # шпаргалка по выражениям
./gradlew :wdl-cli:run --args="examples/language/control-flow.wdl"         # ветвления и циклы
./gradlew :wdl-cli:run --args="examples/language/slices.wdl"               # срез a[1..3] и индекс с конца
./gradlew :wdl-cli:run --args="examples/language/functions.wdl"            # функции и замыкания
.\gradlew :wdl-cli:run --args="examples/language/defaults.wdl"             # значения по умолчанию
./gradlew :wdl-cli:run --args="examples/language/named-args.wdl"           # именованные аргументы
./gradlew :wdl-cli:run --args="examples/language/variadic-args.wdl"        # *args, **named, раскрытие
./gradlew :wdl-cli:run --args="examples/language/const.wdl"                # константы
./gradlew :wdl-cli:run --args="examples/classes/class-varargs.wdl"        # остаток в заголовке класса
./gradlew :wdl-cli:run --args="examples/classes/properties.wdl"           # свойства: get/set, скрытое поле
./gradlew :wdl-cli:run --args="examples/metaprogramming/decorators.wdl"           # декораторы: @[...], like
./gradlew :wdl-cli:run --args="examples/metaprogramming/annotations.wdl"          # аннотации: @{...}, annotations
./gradlew :wdl-cli:run --args="examples/concurrency/threads.wdl"              # потоки и synchronized
./gradlew :wdl-cli:run --args="examples/concurrency/threads_pool.wdl"         # пул, канал, защёлка
./gradlew :wdl-cli:run --args="--metrics examples/modules/plain.wdl" # время стадий
./gradlew :wdl-cli:run --args="--profile examples/tooling/profiling.wdl"  # горячие функции
./gradlew :wdl-cli:run --args="--ast examples/language/hello.wdl"          # показать дерево
./gradlew :wdl-cli:run --args="--tokens examples/lexer/lexer-check.wdl" # показать токены
./gradlew :wdl-cli:run --args="--catalog"                         # что доступно скриптам
./gradlew :wdl-cli:repl --console=plain     # REPL (нужен живой stdin; имена — :names)
./gradlew :wdl-cli:installDist   # готовый запускаемый дистрибутив
                                 # → wdl-cli/build/install/wdl/bin/wdl
./gradlew :wdl-lsp:installDist   # языковой сервер для редакторов
                                 # → wdl-lsp/build/install/wdl-lsp/bin/wdl-lsp
./gradlew :wdl-dap:installDist   # адаптер отладки для редакторов
                                 # → wdl-dap/build/install/wdl-dap/bin/wdl-dap
./gradlew -p ide/idea-plugin buildPlugin   # плагин IntelliJ IDEA (отдельная сборка)
```

## Старый способ запуска через External Tools

Рекомендуемый способ — Run-конфигурация **wdl** из плагина (см. инструкцию выше).
Старый Windows-скрипт также остаётся доступен для External Tools:

[run-wdl.bat](run-wdl.bat)
```
- Program: $ProjectFileDir$\run-wdl.bat
- Arguments: "$FileName$" "$FileDir$" UTF-8
- Working directory: $FileDir$
```

Ключи интерпретатора (`--metrics`, `--profile`, `--ast`, `--tokens`, `--debug`)
дописываются в эту строку где угодно — всё, что начинается с дефиса, уходит `wdl`,
а позиционными остаются файл, каталог и кодировка:

```
- Arguments: --metrics "$FileName$" "$FileDir$" UTF-8
```

## Состояние

Работает конвейер `исходник → токены → AST → выполнение`:

```
$ wdl examples/language/hello.wdl
Привет, wdl!
Итого: 360 руб.
болт по 12 руб., на складе 100 шт.
Стоимость склада: 3250 руб.
...
```

Готовы лексер (`ru.wds.wdl.lexer`), диагностика (`ru.wds.wdl.diagnostic`), значения
(`ru.wds.wdl.value`), AST (`ru.wds.wdl.ast`), парсер (`ru.wds.wdl.parser`), формы классов
(`ru.wds.wdl.resolve`) и интерпретатор (`ru.wds.wdl.runtime`). Скрипт — это присваивания,
распаковка (`x, y = *point`, `host, port, **rest = **config`, остатки `*rest`/`**rest`,
отказ от остатка `*_`, обмен `a, b = b, a` и пропуск `_`, читать который нельзя),
объявления констант (`const`), вызовы (`println`, `print`, `typeof`, `len`),
блоки, ветвления и циклы (`if`, `while`,
`for`, `for ... in` — в том числе с ключом, `for (i, tile in row)` и
`for (k, v in config)`, — `break`, `continue`), ветвление по предмету (`match` с ветками
`case`, образцами из обычных операторов, перечислением `case 1, 2, 3`, условием
`case > 90 if !throttled` и `else` — выражением и инструкцией; ветка в несколько
строк отдаёт значение словом `yield`),
принадлежность (`in`, `has`, `!in`, `!has`, `!is`) и диапазоны (`1..5` — значение,
годное для `in`, для перебора, для передачи в функцию и для среза: `a[1..3]`
и `s[0..2]` с включительными границами, рядом с отрицательным индексом `a[-1]`
и записью «от второго до последнего» `a[1..-1]` — [docs/access.md](docs/access.md)), свои функции (`def`, `return`,
тело-выражение `=>`, анонимные функции, значения параметров по умолчанию,
именованные аргументы `f(count: 2)`, вариативные параметры `*args`/`**named`
и раскрытие `f(*array)`, параметр-дырка `def onClick(_, event)`, замыкания),
классы (`class`, `trait`, `new`, `this`, `super`, `with`, `is`, фабрики
`def Имя.член(...)`, остаток в заголовке `class Proxy(*args, **named)`,
свойства `property имя { def get() def set(value) }` со скрытым полем `field`
и требованиями трейтов через возможность),
одиннадцать дескрипторов типов в корневой области (`Number`, `String`, `Array`,
`Range`, …) —
`typeof` возвращает дескриптор, а не строку, и проверять тип значения теперь можно
через `is` наравне с проверкой класса ([docs/types.md](docs/types.md)),
перегрузку операторов (`` def `+`(right) `` и `` mirror def `/`(left) `` в классе,
трейте и `extend`; один член `` `<=>` `` на четыре сравнения и сортировку, один
`` `==` `` на равенство, один `` `in` `` на четыре записи принадлежности; унарные
`` def `-`() `` — [docs/expressions.md](docs/expressions.md)),
члены значений (`text.upper`, `a.sort()`, `213.toString()`, `box.keys`; обработка
массива — `a.map(f)`, `a.filter(f)`, `a.reduce(f)`, `a.find(f)`, `a.any(f)`, `a.all(f)`,
`a.each(f)` и свойства `a.sum`, `a.min`, `a.max`; интроспекция
`total.params`, `Circle.parent`, `Printable.requirements`; путь в обход данных
`Object.size(box)`; свои члены — `extend Array { ... }` из скрипта и `MemberRegistry`
из приложения — [docs/members.md](docs/members.md)),
декораторы (`@[выражение](аргументы)` на функции, классе и трейте; метаданные
первым аргументом, модуль `sys.meta`, встроенная `like`),
аннотации (`@{ключ: значение}` на функции, классе, трейте, методе, свойстве, фабрике
и параметре; блоки сливаются в один объект, читаются снимком через `annotations`,
рядом с ними `Cls.method(имя)` и `Cls.property(имя)` — [docs/annotations.md](docs/annotations.md)),
ошибки и ресурсы (иерархия `Exception`, `throw`,
`try`/`catch`/`finally`, короткие формы `try?` и `try!`, `defer`, `use` с трейтом
`Closeable`, путь по скрипту в сообщении) и модули (`import lib.math`, `import lib.math as m`,
наследование и трейты через файлы, в том числе от того, что дало выражение —
`: registry["Shape"](...)`). Модуль читается тогда, когда выполняется его
`import`, — поэтому импортировать можно файл, которого при запуске ещё не было.
Состояние модуля общее и на чтение, и на запись: снаружи с ним можно ровно то же,
что можно его собственному коду, а `const` в нём остаётся `const`.
Модулем бывает и библиотека на Java: консольный запуск даёт `sys.io` (файлы),
`sys.json`, `sys.meta`, `sys.net.http`, `sys.net.socket`, `sys.gui`, `sys.thread`
и `sys.time` (весь `java.time` — семь типов, открытых мостом, без единого
написанного метода),
а приложение — свой набор, потому что состав встроенных модулей задаёт запуск,
а не язык.
Отдельным модулем сборки живёт `wdl-game` (WDGame): движок на Swing — окно, сцена
со спрайтами, цикл кадров и холст с попиксельным рисованием — и пинг-понг на нём.
Сама игра написана на Java, а на wdl написаны её правила: подача, отскок от ракетки,
соперник, конец партии и надписи поверх кадра. Четыре скрипта лежат в ресурсах jar
и меняются без пересборки игры. В стандартную библиотеку модуль `game` не входит
и не войдёт: это площадка, на которой встраивание проверяется самым требовательным
приложением из возможных — игра зовёт скрипт шестьдесят раз в секунду и ждёт ответа
до следующего кадра. Запуск — `./gradlew :wdl-game:run`, подробности —
[docs/game.md](docs/game.md).
Работают настоящие потоки: `sys.thread` даёт `spawn`/`join`/`interrupt`, пул
с обещаниями (`pool.map`, `pool.submit`), замок, счётчик, канал и защёлку,
а модификатор `synchronized def` склеивает несколько обращений в одно неделимое.
Обещание языка короткое: одно обращение атомарно, выражение целиком — нет
(см. [docs/threads.md](docs/threads.md)). Приложение зовёт функции скрипта
из любого числа потоков одновременно — прежнего «вызовы выстраиваются в очередь»
больше нет ни как поведения, ни как режима.
Запуск можно ограничить: шаги, таймаут, квота потоков и вложенность вызовов
задаются одним `Limits` — из приложения (`WdlEngine.builder().maxSteps(...)`)
или флагами консольного `wdl` (`--max-steps`, `--timeout`, `--max-threads`).
По умолчанию пределов нет, а набор `Stdlib.SAFE` ставит свои сам — и именно поэтому
`sys.thread` теперь в него входит: `for (;;) th.spawn(...)` упирается в квоту, а
`while (true) {}` — в шаги и время. Остановка по лимиту не ловится обработчиком,
но `defer` и `use` отрабатывают ([docs/limits.md](docs/limits.md)).
Чужой Java-класс отдаётся скрипту без описания руками: `wdl-bridge` снимает
рефлексией методы, поля и конструкторы и ставит тип в область видимости обычным
классом — `new`, точка, `is` и печать работают как у классов языка, а готовый
Java-объект заворачивается тем же мостом (`bridge.wrap(instant)`). Что именно видно,
решает схема, и по умолчанию не видно ничего, чего не открыли: `getClass` не виден
никогда, а неизвестный тип получает отказ с подсказкой, а не молчаливую обёртку
(см. [docs/java-interop.md](docs/java-interop.md)).
Стандартная библиотека (`wdl-stdlib`) даёт `pow`, `sqrt`, `abs`,
классы `File` и `Random` и заодно служит примером
[встраивания](docs/embedding.md). Модуль на Java может отдать скрипту не только
функции и классы, но и трейт-контракт (`class Echo(name) with net.Handler` —
требования проверяются на строке `class`, как у трейтов языка), а свои классы
выстраивать в иерархию. Имена, которые библиотека кладёт в область, собираются
на запуск, поэтому между запусками в одном процессе не переносится ничего.
Время стадий движок считает по просьбе: `wdl --metrics script.wdl` печатает,
во что ушёл запуск, а встраивающее приложение включает то же одной строкой сборки
(`.metrics(true)`) и получает отчёт геттерами — см. [docs/metrics.md](docs/metrics.md).
Выключенные метрики не стоят ничего.
Тем же способом включается профиль: `wdl --profile script.wdl` показывает, какая
функция горячая и сколько времени она провела сама, а не в том, что позвала;
`--profile-out profile.json` отдаёт то же машине, приложение просит `.profile(true)`
и читает `instance.profile()` — см. [docs/profiling.md](docs/profiling.md). Профиль
считает каждый вызов и оттого замедляет скрипт, поэтому он отдельный флаг, а не часть
метрик; выключенный не стоит ничего.
Отладчик подключается к запуску из приложения: `script.debugger()` даёт сессию
с точками останова, кадрами, шагами и вычислением выражения в кадре, причём
подключиться можно и к уже работающему скрипту — на этом стоит режим `attach`.
Останавливаются потоки не насильно, а на своих безопасных точках, пауза не расходует
таймаут, а выключенная отладка стоит чтения `boolean` (замер: +0,6 % при разбросе
в серии 8–10 %) — см. [docs/debugging.md](docs/debugging.md). Из редактора то же
самое доступно по протоколу DAP: адаптер `wdl-dap` запускает скрипт у себя и
разговаривает с IDE, а приложение со встроенным движком открывает отладку живого
запуска строкой `WdlDebugServer.listen(порт, script)`.
Слова `has` и `yield` стали ключевыми — скрипт с переменной по такому имени
перестанет разбираться; обращение `obj.has(k)` при этом работает как прежде:
после точки ключевое слово читается как имя члена.
Выборочного импорта отдельных имён ещё нет, наследоваться от класса встроенного
модуля пока нельзя. У `match` нет структурного сопоставления с извлечением имён
(`case {type: "click", x, y}`), у диапазона — шага (`1..10 by 2`). У декораторов нет короткой формы `@reg` без скобок и нельзя
повесить их на анонимную функцию в позиции выражения — и то и другое аддитивно
и отложено до второй версии; на членах типа декораторов тоже пока нет, а вот
аннотации там работают. Аннотации не вешаются на переменную и на файл, не наследуются
классом и не переносятся мостом из Java-аннотаций.
Подробности — в [docs/statements.md](docs/statements.md),
[docs/control-flow.md](docs/control-flow.md), [docs/functions.md](docs/functions.md),
[docs/classes.md](docs/classes.md), [docs/decorators.md](docs/decorators.md),
[docs/annotations.md](docs/annotations.md),
[docs/errors.md](docs/errors.md),
[docs/modules.md](docs/modules.md), [docs/threads.md](docs/threads.md),
[docs/metrics.md](docs/metrics.md), [docs/profiling.md](docs/profiling.md)
и [docs/expressions.md](docs/expressions.md).

Решения парсера и интерпретатора, определяющие остальное:

* **Выражения разбираются алгоритмом Пратта**, приоритеты лежат таблицей
  в `Operators` — новый оператор это одна строка, а не новый уровень методов.
* **AST — только данные**: `record`'ы в `sealed` иерархии, без `eval()` и без ссылки
  на окружение. Выполнение — посетители `ExprVisitor` и `StmtVisitor`, и таким же
  посетителем написан дамп дерева.
* **Присваивание — инструкция**, а не выражение: `if (x = 5)` не разберётся вообще.
  Составное `+=` не разворачивается при разборе, поэтому `таблица[ключ()] += 1`
  вызывает `ключ()` один раз.
* **Функция — значение с замыканием на область объявления.** Отсюда даром получаются
  рекурсия, вложенные функции и замыкания над изменяемой переменной. Объявление
  существует со своей строки — стадии до выполнения в языке нет, — но тело функции
  спокойно зовёт то, что объявлено ниже: имя ищется в момент вызова.
  Значение параметра по умолчанию считается **при каждом вызове**, а не один раз при
  объявлении, — иначе один массив или объект стал бы общим для всех вызовов, как в Python.
  Имена параметров — часть значения-функции, а не разбора: поэтому `f(count: 2)`
  работает и для своей функции, и для класса, и для встроенной, которая имена объявила.
* **`break`, `continue` и `return` — сигналы**, а не проверяемый в каждом узле код
  возврата: `return` из глубины циклов не требует от циклов ни строчки. Бесконечная
  рекурсия при этом ошибка скрипта, а не `StackOverflowError` в чужом приложении.
* **Ошибка не прерывает разбор**: дерево возвращается всегда, испорченная строка
  пропускается до следующей, а до интерпретатора такое дерево не доходит.
* **`import` — инструкция, а не подготовка**: файл модуля читается тогда, когда
  до этой строки дошло выполнение. Класс связывается с родителем там же, где объявлен,
  поэтому наследоваться можно от класса из модуля, которого при запуске не существовало,
  а импорт в невызванной функции не стоит ничего.
* **Вывод — зависимость**, а не `System.out`: `println` пишет туда, куда указал
  вызывающий (`Output` в `ExecutionContext`), по умолчанию — никуда.
* **Глобального замка нет.** Внутри запуска потоки работают одновременно, а
  корректность держат конкурентные структуры: области видимости, объекты, массивы,
  реестр модулей и формы классов. Обещание автору скрипта — «одно обращение атомарно,
  выражение целиком нет», и это ровно та граница, которую движок может держать
  без того, чтобы отнять параллельность.

Решения лексера, влияющие на грамматику:

* **Перевод строки — обычный пробел.** Токена он не порождает: форматирование на
  разбор не влияет, `def f(a, b)⏎ return a + b` и `def f(a, b) return a + b` — один
  и тот же код. Границы инструкций выводит парсер из грамматики. Факт переноса
  всё же сохранён — в флаге `Token.afterNewline()`, для форматтера и диагностики.
* **`;` — необязательный разделитель** и обычный токен. Нужен там, где инструкции
  стоят в одной строке, и в заголовке `for (i = 0; i < n; i++)`.
* **Ошибка не прерывает разбор.** Всё найденное копится в `Diagnostics`,
  за один запуск видно все проблемы файла.
* **Точка после числа не съедается**, если за ней не цифра: на этом держатся
  и `3.75.round()`, и диапазон `1..5` — он разбирается как `INT`, `..`, `INT`
  без единого исключения в лексере.
* **Цифры только ASCII**, а буквы в именах — любого алфавита: имена на кириллице разрешены.
* **Второй режим — для редактора.** `LexerMode.LOSSLESS` кладёт в поток пробелы,
  комментарии и неизвестные символы, покрывая документ встык от начала до конца;
  режим по умолчанию не изменился ни на строку. Посмотреть: `wdl --tokens --trivia файл`.

Что готово для инструментов — подсветки, перехода к объявлению, форматтера, LSP
(подробно в [docs/tooling.md](docs/tooling.md)):

* **позиции — публичный контракт**: `Span` в единицах UTF-16, полуинтервал, перевод
  «смещение ↔ строка:столбец» обратим в обе стороны (`Source.offsetOf`);
* **дерево адресуемо целиком**: части узлов со своим местом в тексте (параметр,
  аргумент, ветка `match`, аксессор) — это `Fragment`, третья ветвь `Node`;
* **обход один на весь проект**: `ast.Nodes` — `children`, `walk`, `deepestAt`,
  `pathAt`, `nameSpanOf`;
* **интервалы проверяются машиной**: ребёнок вложен в родителя, соседи не пересекаются
  — по всем примерам и по корпусу нарочно недописанного текста;
* **у объявлений есть место имени** (`nameSpan`) — то, к чему ведёт переход
  к объявлению и что меняет переименование;
* **у диагностик есть код** (`DiagnosticCode`), а у области — перечисление имён
  (`Environment.names()`);
* **имена анализируются без запуска**: `wdl-tools`, пакет `ru.wds.wdl.tools.analysis`,
  точка входа `FileAnalysis` — дерево областей, символы файла, «что видно в этой
  точке», переход к объявлению, поиск употреблений, документация из комментариев.
  Правило видимости повторяет язык: имя существует со своей строки, но тело функции
  видит внешнее целиком — потому и работает рекурсия;
* **имена, которых в файле нет, знает каталог**: `ru.wds.wdl.tools.catalog` —
  встроенное языка, корневая область запуска и состав модулей `sys.*`. Каталог
  **снимается с движка**, а не пишется рядом: имена, сигнатуры и члены берутся
  у живых значений, руками объявляется только описание словами (`.doc(...)`
  в построителе). Посмотреть: `wdl --catalog` и `:names` в REPL.

* **редактор получает всё это по протоколу**: `wdl-tools`, пакет
  `ru.wds.wdl.tools.service` — открытые документы, версии правок, диагностика,
  дополнение, наведение, переход, употребления, структура файла и семантическая
  подсветка обычным Java API; `wdl-lsp` переводит эти ответы в JSON-RPC
  (`wdl-lsp --stdio`), а плагин в `ide/idea-plugin/` подключает сервер к IntelliJ IDEA.
  Правил языка ни в сервере, ни в плагине нет: тем же слоем пользуется приложение
  со своим редактором — напрямую, без сокета. Там же, в плагине, кнопка
  **Профилировать**: она запускает конфигурацию с `--profile-out` и показывает
  горячие функции и дерево вызовов окном IDE — считает по-прежнему `wdl`,
  плагин только читает файл отчёта.

* **отладка идёт по DAP**: `wdl-dap` переводит сессию отладки ядра в протокол
  DAP: точки останова, шаги, кадры, панели переменных, вычисление в кадре.
  Тот же плагин поднимает адаптер по кнопке «Отладить», а приложение со встроенным
  движком открывает отладку живого запуска одной строкой `WdlDebugServer.listen`
  ([docs/debugging.md](docs/debugging.md)).

Дальше по плану — типы и дополнение членов после точки, затем форматтер,
переименование и инспекции (`plans/ПланЯзыковогоСервиса.md`).

Кодировку вывода CLI выбирает сам: кодировка терминала, если запущен в терминале,
иначе UTF-8 (запуск из IDE, перенаправление в файл). Принудительно —
`-Dwdl.console.encoding=cp866`.

## Требования

JDK 21+ (toolchain выставлен в `gradle/libs.versions.toml`), Gradle Wrapper 9.0.

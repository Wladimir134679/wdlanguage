/**
 * WDGame — маленький игровой движок и площадка для проверки встраивания.
 * <p>
 * Модуль устроен как <b>игра</b>: {@link ru.wds.wdl.game.GameLauncher} запускает
 * пинг-понг, а wdl внутри него занят тем же, чем был бы занят в чужой игре, —
 * правилами. Подача, отскок, соперник, конец партии и надписи описаны скриптами,
 * лежащими в ресурсах ({@code ru/wds/wdl/game/pong/*.wdl}); всё остальное — поле,
 * ввод, столкновения, счёт — обычный Java-код.
 * <p>
 * Игра выбрана не случайно: это самое требовательное приложение из тех, что зовут
 * скрипт, — шестьдесят раз в секунду, из своего цикла, с ожиданием ответа
 * до следующего кадра. Всё, что в таком режиме окажется неудобным или дорогим,
 * должно найтись здесь.
 *
 * <h2>Четыре слоя и границы между ними</h2>
 * <table border="1">
 *   <caption>Кто что знает</caption>
 *   <tr><th>Пакет</th><th>Что там</th><th>Знает ли про wdl</th></tr>
 *   <tr><td>{@code engine}</td><td>сцена, спрайты, ввод, окно, цикл кадров, холст</td>
 *       <td>нет</td></tr>
 *   <tr><td>{@code pong}</td><td>сама игра и её пять вопросов к правилам</td>
 *       <td>нет</td></tr>
 *   <tr><td>{@code script}</td><td>типы {@code Sprite}, {@code Canvas}, {@code Match}
 *       для скрипта</td><td>да</td></tr>
 *   <tr><td>{@code ru.wds.wdl.game}</td><td>модуль {@link ru.wds.wdl.game.Games},
 *       встроенные скрипты {@link ru.wds.wdl.game.PongScripts}, запуск</td>
 *       <td>да</td></tr>
 * </table>
 * Снять два верхних слоя, не тронув двух нижних, можно в любой момент — игра
 * останется играбельной с хуками на Java. Ради такой проверки всё и написано.
 * <p>
 * Описание для того, кто правит правила, — в {@code docs/game.md}.
 */
package ru.wds.wdl.game;

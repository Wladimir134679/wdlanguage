/**
 * Значения времени выполнения: число, строка, булево, null, массив, объект, функция,
 * обёртка над Java-объектом.
 * <p>
 * Булево — самостоятельный тип, а не 0/1: иначе сравнения и приведения типов
 * превращаются в источник вечных сюрпризов.
 */
package ru.wds.wdl.value;

/**
 * Лексический анализ: превращение текста в поток токенов.
 * <p>
 * Каждый токен несёт {@link ru.wds.wdl.source.Span}. Лексер не выбрасывает исключение
 * на первой ошибке, а складывает диагностику в {@code ru.wds.wdl.diagnostic} и продолжает —
 * чтобы пользователь увидел все проблемы за один запуск.
 */
package ru.wds.wdl.lexer;

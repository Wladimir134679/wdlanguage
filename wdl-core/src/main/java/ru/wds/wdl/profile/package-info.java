/**
 * Профилировщик: какая функция скрипта самая горячая.
 * <p>
 * Отвечает на вопрос, которого нет у {@link ru.wds.wdl.metrics метрик}: те считают
 * время четырёх стадий конвейера и стоят ноль, этот считает вызовы и стоит двух
 * обращений к часам на каждый. Отсюда и разные способы включения: метрики — флагом
 * {@code --metrics}, профиль — флагом {@code --profile}, и по умолчанию нет ни того,
 * ни другого.
 * <p>
 * Точек съёма ровно столько, сколько точек шага у лимитов, и стоят они там же:
 * вызов функции скрипта, создание экземпляра, вызов чужого кода и верхний уровень
 * файла. См. {@code docs/profiling.md}.
 */
package ru.wds.wdl.profile;

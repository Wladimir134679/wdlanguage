/**
 * Абстрактное синтаксическое дерево — <b>только данные</b>.
 * <p>
 * Ключевое правило: у узлов AST нет метода {@code eval()}. Выполнение живёт
 * в {@code ru.wds.wdl.runtime}, а состояние передаётся аргументом. Именно это
 * позволяет распарсить скрипт один раз и запускать его многократно и параллельно
 * в независимых окружениях.
 * <p>
 * Узлы — {@code record}'ы в {@code sealed} иерархиях, поэтому обход выражается
 * через pattern matching в {@code switch} без ошибок «забыл ветку».
 */
package ru.wds.wdl.ast;

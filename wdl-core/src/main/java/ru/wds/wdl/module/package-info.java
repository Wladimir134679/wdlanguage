/**
 * Файлы и связи между ними: разбор модулей и то, откуда брать их исходники.
 * <p>
 * {@link ru.wds.wdl.module.Unit} — один разобранный файл,
 * {@link ru.wds.wdl.module.ModuleUnits} — реестр таких файлов на запуск,
 * {@link ru.wds.wdl.module.ModuleSource} — то, что приложение задаёт само.
 * <p>
 * <b>Читается файл только тогда, когда выполняется его {@code import}.</b> Заранее
 * не читается ничего: подготовка главного скрипта не знает про модули вовсе, и
 * поэтому модуля может не быть на диске в момент запуска — его создаст приложение,
 * сгенерирует другой скрипт, отдаст база. Разбор модуля точно так же не тянет за собой
 * его импорты: они обычные инструкции, до которых дойдёт очередь.
 * <p>
 * Выполнением здесь не занимаются: значения модулей собирает {@code runtime}, беря
 * разобранные юниты отсюда.
 */
package ru.wds.wdl.module;

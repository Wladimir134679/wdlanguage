/**
 * Измерение времени стадий конвейера: лексер, парсер, выполнение, закрытие запуска.
 * <p>
 * Приёмник {@link ru.wds.wdl.metrics.Metrics} передаётся движку так же, как вывод
 * и диагностика, — аргументом, а не через статику: иначе два интерпретатора в одном
 * процессе считали бы в одну кучу. По умолчанию метрики выключены и стоят ноль
 * ({@link ru.wds.wdl.metrics.Metrics#off()}).
 * <p>
 * Стадию меряет тот, кто её запускает: сами лексер и парсер об этом пакете не знают.
 * Исключение — модули: их разбор и выполнение случаются внутри движка, снаружи туда
 * не дотянуться, поэтому приёмник приходит в {@code ModuleUnits} и в контекст запуска.
 * <p>
 * Это не профилировщик: время отдельного вызова функции здесь не меряется — у такой
 * задачи другая цена и другой способ включения.
 */
package ru.wds.wdl.metrics;

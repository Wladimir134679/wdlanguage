/**
 * Резолвер: стадия между парсером и интерпретатором.
 * <p>
 * Связывает объявления классов и трейтов и проверяет то, что известно до выполнения:
 * невыполненные требования трейтов, круг в наследовании, число аргументов родителю.
 * Результат — {@link ru.wds.wdl.resolve.Resolution}: форма каждого объявления,
 * то есть плоские таблицы полей и методов, цепочка предков и набор трейтов.
 * <p>
 * Разделение, ради которого пакет и существует: <b>форма — имена и порядок,
 * значение — замыкание и данные.</b> Форма не зависит от запуска и общая для всех
 * потоков, значение ({@code runtime.WdlClass}) у каждого запуска своё.
 */
package ru.wds.wdl.resolve;

/**
 * Формы классов и трейтов: плоские таблицы полей и методов, цепочка предков,
 * набор трейтов.
 * <p>
 * Вся работа идёт во время выполнения: {@link ru.wds.wdl.resolve.Linker} собирает
 * форму в тот момент, когда выполняется объявление класса, — когда родитель и трейты
 * уже стали значениями; он же проверяет требования трейтов и число аргументов
 * родителю. Стадии между парсером и интерпретатором нет: по тексту о типах не видно
 * ничего, что стоило бы отдельного прохода, — родитель может прийти из модуля,
 * которого в момент запуска ещё не существует.
 * <p>
 * Разделение, ради которого пакет и существует: <b>форма — имена и порядок,
 * значение — замыкание и данные.</b> Значение ({@code runtime.WdlClass}) знает
 * замыкание и данные, форма — состав и порядок, и на её единственности держится
 * {@code is}: класс, объявленный внутри функции, при каждом вызове даёт новое
 * значение, но остаётся тем же классом.
 */
package ru.wds.wdl.resolve;

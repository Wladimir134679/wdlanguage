/**
 * Формы классов и трейтов: плоские таблицы полей и методов, цепочка предков,
 * набор трейтов.
 * <p>
 * Работы здесь две, и разнесены они во времени.
 * {@link ru.wds.wdl.resolve.Resolver} работает между парсером и интерпретатором
 * и отвечает на единственный вопрос, ответ на который виден из текста: какие
 * объявления типов можно выполнить до первой инструкции и в каком порядке.
 * {@link ru.wds.wdl.resolve.Linker} работает во время выполнения и собирает саму
 * форму — когда родитель и трейты уже стали значениями; он же проверяет требования
 * трейтов и число аргументов родителю.
 * <p>
 * Разделение, ради которого пакет и существует: <b>форма — имена и порядок,
 * значение — замыкание и данные.</b> Значение ({@code runtime.WdlClass}) знает
 * замыкание и данные, форма — состав и порядок, и на её единственности держится
 * {@code is}: класс, объявленный внутри функции, при каждом вызове даёт новое
 * значение, но остаётся тем же классом.
 */
package ru.wds.wdl.resolve;

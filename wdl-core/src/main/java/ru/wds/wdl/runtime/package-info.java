/**
 * Выполнение: интерпретатор, окружения, кадры вызовов, лимиты.
 * <p>
 * Здесь нет ни одного изменяемого статического поля — это жёсткое правило модуля.
 * Всё состояние висит на экземпляре, поэтому в одной JVM живёт сколько угодно
 * взаимно изолированных интерпретаторов.
 * <p>
 * Окружение спрятано за интерфейсом: сейчас это поиск имени по цепочке областей
 * видимости, позже — индексы слотов, вычисленные отдельным проходом-резолвером.
 * Замена не должна затронуть интерпретатор.
 */
package ru.wds.wdl.runtime;

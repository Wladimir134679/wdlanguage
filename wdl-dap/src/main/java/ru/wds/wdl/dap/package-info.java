/**
 * Адаптер отладки: перевод сессии {@link ru.wds.wdl.debug.DebugSession} в JSON-RPC
 * протокола DAP.
 * <p>
 * Правил отладки здесь нет ни одной строки — и это требование к модулю, а не
 * наблюдение. Останавливает потоки, считает кадры и вычисляет выражения ядро;
 * всё, что окажется записано здесь, потеряется для приложения, которое отлаживает
 * свой встроенный движок само.
 * <p>
 * Обратное правило тоже держится: классов lsp4j нет нигде, кроме этого модуля
 * и {@code wdl-lsp}, и проверяет это компилятор — в ядре и в инструментах
 * библиотеки нет на пути классов.
 */
package ru.wds.wdl.dap;

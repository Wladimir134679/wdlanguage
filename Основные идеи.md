- Язык динамически-типизированный.
- Всё объект, всё переменная. Всё можно положить в переменную
- wdl - сокращение названия языка своего
- Типы данных: число, строка, булеен значение, массив, объект, функция
- 

## Примеры кода с пояснениями
Код, который будет, есть желание видеть такие конструкции

```wdl
nameVarString = "Строка тут" // комментарий
a = 5
b = 63.2
c = a + b // c = 68.2
println("C = " + c) // вывод в консоль
println("C = ", c) // вывод в консоль, аргументы друг за другом без конкатенация срок
```

Для функций

> Сделано. Что получилось — в [docs/functions.md](docs/functions.md), рабочий пример —
> в [`examples/functions.wdl`](examples/functions.wdl). Отличие от задуманного одно:
> **после `return` обязательна точка с запятой**. Без неё `return` и следующая строка
> неразличимы — перевод строки токена не даёт, — и движку пришлось бы молча выбирать
> за автора одно из двух толкований. Тело-выражение `=>` терминатора не требует,
> так что самая короткая запись осталась ровно такой, как задумывалась.

```wdl
fun name(a, b){
    return a + b;
}
fun nameOneBlock(a, b) 
    return a + b;
    
// Одно и тоже
    
fun nameOneBlock2(a, b) return a + b;

fun nameOneBlock3(a, b) => a + b
    
println("result sum = ", name(1, 2))
println("result sum = ", nameOneBlock(1, 2))
println("result sum = ", nameOneBlock2(1, 2))
println("result sum = ", nameOneBlock3(1, 2))

funName = fun(a, b)
    return a + b;

println("Результат переменной функции = ", funName(1, 2))


fun name(a, b) { return a + b; }    // блочное тело
fun name(a, b) return a + b;        // тело — одна инструкция (перенос строки не значим)
fun name(a, b) => a + b             // тело-выражение, => вместо return
f = fun(a, b) => a + b              // анонимная в выражении
f = fun(a, b) { return a + b; }     // анонимная с блоком
```

## Старая реализация

Пример кода, который был в моём прошлом проекте. Были классы, структуры данные, модули, библиотеки

```wdl
import logger as log

log.init("other program", "${date} [${thread}] ${level} ${module} - \t ${message}")

log.info("Start test other program")

log.info("Create function")
def testFunc(args, a = 10, b = 15){
    log.info("Вызов функции: testFunc, args = " + args + " a = " + a + " b = " + b)
    return a * b
}
log.info("Execute function testFunc = " + testFunc("ARGSSTRING", 2))

log.info("End test other program")
```

Вот такие классы были

> Сделано, но иначе. Что получилось — в [docs/classes.md](docs/classes.md), рабочий
> пример — в [`examples/classes.wdl`](examples/classes.wdl). Отличий от задуманного
> три, и все они из одного решения — **заголовок класса и есть список полей**:
> `class Point(x = 0, y = 0)` вместо конструктора, который поля заводит. Отсюда
> `fun` вместо `def`, поле по имени без `this` (`x += dx`, а не `this.x += dx`)
> и конструктор — необязательный метод с именем класса, для проверок и производных
> полей. Появилось и то, чего в задумке не было: типажи, `super` и `is`.

```wdl
VAR_POP = "Эта переменная из classes"

class Point {

  def Point(x = 0, y = 0) {
    this.x = x
    this.y = y
  }

  def move(dx, dy) {
    this.x += dx
    this.y += dy
  }

  def toString() = "(" + this.x + ", " + this.y + ")  " + VAR_POP
}

p = new Point(20, 30)
p.move(10, -5)
println(p.toString())


p2 = new Point(10, -5)
p2.move(10, -5)
println(p2.toString())
```
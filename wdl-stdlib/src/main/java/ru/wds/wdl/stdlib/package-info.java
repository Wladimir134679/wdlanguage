/**
 * Стандартная библиотека языка.
 * <p>
 * Каждый модуль ({@code math}, {@code string}, {@code array}, {@code io}, {@code time})
 * — это обычное значение-объект с полями-функциями, которое кладётся в переменную
 * при {@code import}. Отдельных правил в грамматике для них нет.
 * <p>
 * Модули делятся на профили: {@code SAFE} (чистые вычисления, безопасно для
 * недоверенных скриптов) и {@code FULL} (файлы, сеть, процессы).
 */
package ru.wds.wdl.stdlib;
